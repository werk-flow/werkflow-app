# P1-24a — Plantafel And The Calendar Overhaul

Status: living — last reviewed 2026-09-18; slice plan written before implementation on 2026-09-15 and extended on 2026-09-18 to the whole calendar on the owner's instruction, the slice has not started

## Read This First

This record is the plan of the next slice. It grew in two steps: on 2026-09-15 pre-Wave-3 step 3 placed the `Plantafel` here as the week view's successor; on 2026-09-18, after the production release, the owner extended the slice to every calendar view, because the calendar is the office's main hub and its current state ("cropped corners, a stray scrollbar, an off-centre now dot, thick low-contrast lines, sluggish non-optimistic drags, generic error messages") is not acceptable for the product's most-used surface. The [2026-09-18 audit](#audit-of-2026-09-18) below is the verified evidence for that judgement.

Two rules for the agent that takes this slice:

- This plan is a floor, not a ceiling. It names the defects that are known and the direction that is decided; it does not claim to list every improvement the calendar needs. Run your own discovery on the code and in the browser at every viewport, research the references named below with your own eyes, and propose what this plan missed in the pre-implementation report. A local maximum of "the old views with thinner lines" fails this slice.
- Bold changes are wanted, safeguards are not negotiable. The range owner's freshness contract, the mutation ownership, the authorization of every action, the qualification, capacity, supersession, commitment and series dialogs, and the catalog clauses that describe today's behaviour all stay. What changes is the rendering, the interaction engine, the visual language, the optimistic behaviour of every drop, and the words the user reads when something is refused.

Before changing anything: load the `werkflow-design` skill, read the [calendar spec](../../../features/calendar-and-resource-planning.md) including its open decisions, the [freshness contract](../../../technical/realtime-and-caching.md), the [step 3 record](../pre-wave-3/03-clock-fab-and-calendar-ux.md#the-calendar-and-the-plantafel) (the board discovery and the market table), and the P1-11, P1-12, P1-14, P1-21 and P1-22 slice records under this folder.

## Bounded Outcome

The calendar becomes one coherent, fast planning surface built on one rendering and interaction layer:

- The week view becomes the `Plantafel`: a people-row board over one to six weeks with multi-day bars, absences, holidays, closure days and capacity in the row, dispatch state on every card, drag-and-drop dispatch between rows and days with keyboard and form alternatives, the Parkplatz as a drag source, notes, a read-only mode and per-user persistence. It stores nothing new and calls only existing actions. Employees see the same component with their own single row.
- The day view and the month view are rebuilt on the same layer: one grid language (hairlines from tokens, no bordered cell cards, no clipped corners, no stray scrollbars, one now indicator), one drag engine that moves a card under the pointer without a React commit per frame, and drops that are optimistic in every view under the range owner's mutation ownership, with Undo after persistence and rollback with a specific message on failure.
- Every calendar action that can be refused tells the user which rule refused it and what to do next, in German, at the point of action; no generic "konnte nicht" message and no raw error code survives.
- The calendar is measured at interaction level (drag, drop, resize, view switch) and holds reviewed budgets on the typical profile, at 1280×720 and 2560×1440 alike.

The owner decided on 2026-09-15 that the board replaces the week view rather than sitting beside it, and on 2026-09-18 that the day and month views and every calendar message belong to the same slice; the discovery and the market evidence are in [pre-Wave-3 step 3](../pre-wave-3/03-clock-fab-and-calendar-ux.md#the-calendar-and-the-plantafel) and in the [research](#research-of-2026-09-18) below.

## Primary User And Roles

- `admin` and `buero` plan on the board and in the day view: they see every active employee, drag, resize, park, unpark, reassign, add notes and switch the read-only mode. The month view is their overview of absences, holidays, closures and load per day.
- `employee` sees the board as "Woche" with exactly their own row; no drag, no other people, no capacity of others. The day and month views show only their own occurrences and time, as today.
- Outsiders and members of another organization see nothing of the calendar's data.

## Verified Current Baseline

### Verified on 2026-09-15 in the code

- `components/kalender/week-view/week-view.tsx` (893 lines) is a people-row day board: one row per member, seven day columns, a `min-w-[1150px]` grid with a sticky name column, up to three time blocks and three job badges per cell with "+n mehr", unassigned jobs in the day header, drag of job badges between rows and days and from the Parkplatz, drag of closed time sessions, a cell click that opens the member-day dialog. Managers see the members selected in the filter; employees see only themselves.
- The week view receives no absence, holiday, closure or capacity data. Absences render in the month view only. Capacity exists per planning action (`CapacityEvaluation` in `lib/planning/types.ts`), not per displayed window.
- `components/kalender/use-calendar-range-data.ts` and `GET /api/calendar-window` already return occurrences, time entries, vacation, sickness and holiday context for a window. `PlanningCalendarEntry` carries no dispatch state; the Einsätze panel reads it through `getDispatchOverview`.
- Only the month view uses FullCalendar (`calendar-container.tsx:1609`); the day and week views are hand-rolled and share the container's range owner, mutation ownership and drag state.

### Audit of 2026-09-18

Read-only audit of `components/kalender/**`, `lib/calendar/**`, the calling actions, `app/globals.css` and the freshness contract, plus screenshots and DOM probes of the demo organization on the local dev server at 1920×1080, 1280×720 and 375×812 in both themes (Playwright, `deviceScaleFactor` 2). Line numbers are from commit `5280332`.

**Architecture.** Month is FullCalendar (`calendar-container.tsx:93-99`, `:1609`; `fullcalendar-view.tsx:1773-1819`). Day (`day-view/day-view.tsx`, 1,922 lines) and week (`week-view/week-view.tsx`) are hand-rolled. The container is one 1,952-line client component with about 25 `useState`s, three floating panels and every drop handler. The range owner (`use-calendar-range-data.ts`, reducer in `lib/calendar/range-data.ts`) reads five datasets through one GET; `beginMutation` (`:369-383`) bumps every dataset generation, counts ownership and returns an idempotent release; release schedules one authoritative window read after `REALTIME_DEBOUNCE_MS`, during which Realtime echoes coalesce. Three drag mechanisms coexist: pointer events in the day view, HTML5 drag-and-drop in the week view, the Parkplatz and the untimed chips, and FullCalendar's interaction plugin in the month view; payload types are owned by `parkplatz-panel.tsx:25-32` and imported by both views; `drag-state.ts` mutates `document.body`.

**Where a drop is optimistic today, and where it is not.**

| Operation | View | Local state before the server answers? | Where |
| --- | --- | --- | --- |
| Job move in the same row, resize | Day | Yes, a `jobOverrides` map | `day-view.tsx:575-579`, `:607` |
| Job move to another row | Day | Yes | `:1100-1108`, `:1141` |
| Session move or resize in the same row | Day | Yes, `optimisticOverrides` | `:409-413`; then `:495` or a sequential server loop `:517-535` |
| Session to another member | Day | Yes | `:966-983`, `:1021-1043` |
| Job cell drop (date or member) | Week | Yes, straight into the range owner | `calendar-container.tsx:1387-1393`, `:1395` |
| Job header drop | Week | Yes | `:1209-1216`, `:1217` |
| Session cell drop | Week | Yes | `:1525-1539`, `:1541-1550` |
| Job drop on a day | Month | In React yes, but FullCalendar first snaps the card back (`info.revert()` at `fullcalendar-view.tsx:780`, `dragRevertDuration={0}` at `:1816`); the new position appears only when the `events` prop re-renders | `calendar-container.tsx:1284-1294` |
| Unpark | All | The card lands at once (`:879-880`), but success needs two sequential server calls (`:882`, `:947`) and a failure of the second re-parks with a third (`:916-928`) | |
| Park by drag | Day, month | **No.** `handleParkJob` (`:789-814`) only opens the context dialog; the card moves when the dialog submits (`:1004-1030`). Between drop and submit nothing changes on the grid | |
| Schedule an untimed chip | Day | Yes | `:1118-1129` |

Two optimistic layers coexist: the day view keeps its own override maps that self-clear when props catch up (`day-view.tsx:329-344`, `:360-378`); week and month write into the range owner. This is why the owner sees drops that "do not update optimistically": the park drop shows nothing until a dialog is submitted, the month drop visibly snaps back before it moves, and unpark shows the card and then waits on two round trips.

**What one drop costs on the network.** No calendar code calls `router.refresh()` for a drop, but `useRealtimeRouterRefresh` (`calendar-container.tsx:514-520`) refreshes the whole route on member, profile and settings events, and every planning write calls `revalidatePath('/kalender')` (`lib/planning/actions.ts:41`, `lib/work-lifecycle/actions.ts:133`, `lib/dispatch/actions.ts:43`, `lib/time-tracking/actions.ts:650`), so each Server Action response carries a fresh RSC render of the page that re-runs the five server prefetch reads (`app/(app)/kalender/page.tsx:1155-1166`) the client then ignores. On top of that the settle read runs the five-dataset GET plus `getParkedJobs`; `handleJobParked` adds `fetchParkingContexts` and `handleSilentRefresh` (`:1051`, `:1070`), and the `work_blockers` live view re-reads contexts again (`:521-533`). One unpark is two to three Server Action POSTs, each with a server-side page render, one window GET, one parked-jobs POST and one or two parking-context POSTs.

**Performance suspects, ranked by expected impact.**

1. React state on every `pointermove` in the day view: `stableMoveHandler` (`day-view.tsx:717-858`) calls `setActiveDrag` and `setJobDragShadow` per move; `activeDrag` feeds props of every `EmployeeTimelineRow` (`:1694-1727`), rows are not memoized (`employee-timeline-row.tsx:157`), so every row, `WorkSessionBlock` and `JobBlock` re-renders at pointer rate. `useBlockDrag` does the same per block (`use-block-drag.ts:139-143`). Positions are inline `left` and `width` (`job-block.tsx:164-172`, `work-session-block.tsx:973-980`), so each frame also lays out; no `requestAnimationFrame`, no `transform`.
2. The whole week grid re-renders on every `dragover`: `setDragOverTarget` fires per `dragover` and `dragenter` (`week-view.tsx:484-498`, `:326-340`); nothing under `WeekView` is memoized; each render recomputes `createSessionFromCalendarBlock(..., new Date(nowTick))` per visible block inside JSX (`:512-519`) and injects a `<style>` element (`:300`).
3. Grid lines as DOM: each day-view row renders 24 hour-line divs plus 24×(0|1|3) sub-line divs (`employee-timeline-row.tsx:506-524`), and the header repeats them (`timeline-header.tsx:55-88`); the probe counted 1,179 elements on the day view and 1,213 on the month view for 13 members before a single drag. One `repeating-linear-gradient` per row would remove them.
4. Per-render work inside JSX in the day row: `createSessionFromCalendarBlock(block, new Date(), …)` per block per render (`employee-timeline-row.tsx:572-577`); `dayWorkBlocks` depends on `currentTimePosition` and recomputes every minute (`:303`); `formatTime` creates an `Intl` formatter per call (`work-session-block.tsx:100-105`).
5. The network cost per drop above.
6. Month: `allEvents` depends on `nowTick` (`fullcalendar-view.tsx:425`), so FullCalendar diffs the full event set every minute; `withMemberContext` does `members.find` per block (`:249-267`); `renderEventContent` recomputes break minutes per render (`:963-977`); `dayMaxEvents={2}` still mounts every overflow event; a 740-line `<style jsx global>` block is inlined (`:1027-1771`).
7. Container-level churn: `setParkplatzDragCursor` and `setCursorOverParkplatz` on every window `dragover` with two `getBoundingClientRect` and `querySelector` calls (`calendar-container.tsx:308-334`), re-rendering the container and all three views' props; `DayView` and `WeekView` are not memoized.
8. Week `sessionsByMemberDay` filters all entries per member×day (`week-view.tsx:265-283`) every minute; `JSON.stringify` of a full session on every drag start (`:558`, `:754`).
9. `updateScrollbarMetrics` sets two states on parent scroll and on every `ResizeObserver` tick (`day-view.tsx:1293-1318`).
10. Override reconciliation effects (`day-view.tsx:329-344`, `:360-378`) run `entries.find` per override on every entries change.

Measured references today (`lib/testing/performance-baselines.json`, budgets in `lib/testing/measured-scenarios.ts`): day cold open 699 ms (budget 5,000), day to week 395 ms (2,000), week to day 88 ms (500), week to month 1,206 ms (3,000), month next 792 ms (3,000), month to week 120 ms (500). No drag, drop or resize is measured; the sluggishness the owner feels is invisible to the current scenarios.

**Error messages.** The drag and park paths collapse almost every server code into one string: "Auftrag konnte nicht eingeplant werden." (`calendar-container.tsx:960`, `:1141`) and "Auftrag konnte nicht verschoben werden." (`:1226`, `:1305`, `:1403`; `day-view.tsx:624`) cover every `updateJob` and `updatePlanningCalendarEntry` code except `qualification_declined` (folded: `not_authorized`, `job_not_found`, `no_changes`, `update_failed`, `partial_update`, `load_failed`, `not_found`, `employee_not_found`, `member_not_found`, `invalid_occurrence`, `assessment_failed`, `invalid_input`, `calendar_transport_failed`, `calendar_scope_changed`). "Der Parkplatz konnte nicht verlassen werden." (`:897`) folds every `unparkWorkTarget` code. "Eintrag konnte nicht verschoben werden." (`:1558`) has one specific sibling for `overlapping_session`. The day view discards the server's message entirely (`results.push({ success: r.success })`, `:519-521`) although `updateEntry` returns real German text from `validateTimestampUpdate` ("Ein Eintrag existiert bereits um 08:15. Der früheste verfügbare Zeitpunkt ist eine Minute später.", `lib/time-tracking/validation.ts:65-68`; "Zeitstempel kann nicht in der Zukunft liegen.", `:988`). Undo failures are one string for every cause (`:575`, `:593`). `entry-details-dialog.tsx:127-141` maps three codes and otherwise returns the code itself, so a user can read "not_authorized" or "update_failed" verbatim (`:966`, `:981`, `:1226`). `planning-entry-form.tsx:278-280` has one generic string for every non-warning code; `planning-occurrence-edit-dialog.tsx` maps `started_occurrence`, `no_mutable_occurrences` (`:241-243`) and `stale_series` (`:306-307`) and is generic otherwise. The models to copy exist: `DISPATCH_ERROR_MESSAGES` in `lib/dispatch/types.ts:161-225` and `PARKING_ERROR_MESSAGES` in `lib/parking/types.ts:21-33` are full code-to-German maps. Server rules on the drag path for time: same-day overlap with active entries (`validation.ts:36-73`), future timestamp (`:984-990`), day sequence (`:443`), `invalid_time_range` and `overlapping_session` on reassignment (`lib/time-tracking/actions.ts:1910`, `:1928`), the batch's day-sequence check (`:2089-2095`). The UI pre-checks only block overlap (`work-session-block.tsx:429-432`, red preview `day-view.tsx:1786`) and, in the week view only, future dates; a day-view resize into the future fails after the attempt with the generic message. The audit found no closed-period, approved-correction or minimum-duration check on `updateEntry` and `reassignEntries`; the implementing agent verifies whether period close is enforced on that path and, if it is not, records it as a finding for the time-tracking owner.

**Visual and layout defects (seen in the screenshots, located in the code).**

- Month: doubled borders. `--fc-border-color` gives every cell FullCalendar's border (`fullcalendar-view.tsx:1036`), each day cell adds `border: 1px solid … !important` (`:1434`) and `border-collapse: separate` (`:1057-1060`) stacks them; the probe reads `1px solid` on the grid and `1px solid` on every cell at 60 % alpha, which renders as a 2 px lattice.
- Month: cropped corners. `.fc` and `.fc-scrollgrid` both set `border-radius: 0.75rem; overflow: hidden` (`:1051-1052`, `:1070-1074`); the 12 px radius clips the square header and cell borders (visible top-left and top-right of the grid) and breaks the 8 px `--radius` canon.
- Month: the stray scrollbar. At 1280×720 the body scroller overflows (`clientHeight` 513, `scrollHeight` 732) and FullCalendar sets the sticky header's `.fc-scroller` to `overflow-y: scroll` to match the body's gutter, so a 10 px scrollbar track sits to the right of the weekday header; the custom `.fc-scroller` thumb styling (`:1706-1731`) makes it visible. At 1920×1080 the body fits and the track disappears, which is why it looks intermittent. The wrapper also sits inside the page's own `overflow-auto` scroller (`calendar-container.tsx:1699`): nested scroll.
- Month: absence bars render white text on light grey ("Urlaub – Sven Neumann", "Abwesend – Fatih Yıldız" are barely legible in the light theme); 72 `rgb(` and `rgba(` literals with `.dark` overrides for a handful (`:1733-1770`); event fills like `rgb(34 197 94 / 0.8)` do not flip in dark mode; the now line is `--primary` orange, 2 px (`:1361-1364`), while the day view's is red.
- Month: fixed 120 px cells (`:1451`) leave the 1920 px grid mostly empty; "+n mehr" after two events.
- Week: every cell is its own bordered card (`week-view.tsx:471`, `:441`) in a `gap-1` grid, so the board reads as 98 boxes with gutters rather than one grid; the today column is a vivid violet `rgb(123,44,191)` tint and border (`:318`, `:473`, `:791`), the family the design canon bans, with `!important` hover CSS (`:40-52`); `min-w-[1150px]` (`:299`) forces a horizontal scroll at 1280×720 with the sidebar open, while at 1920 the columns stretch and the `min-h-[110px]` cells stay sparse; job chips are tiny at 1920.
- Day: hour lines `border-border/30` and sub-lines `/15` in rows (`employee-timeline-row.tsx:509`, `:515`) versus `/50` and `/20` in the header (`timeline-header.tsx:59`, `:75`), two weights for the same line; fixed 110 px rows (`layout-constants.ts:2`); `text-[10px]` and `text-[9px]` inside blocks (52 sub-11 px font sites across `components/kalender`); the leftmost hour label is cut in half at the scroll start.
- Day: the now indicator. The header draws a 2 px opaque `bg-destructive` bar with an 8 px dot at `-top-0.5` (`timeline-header.tsx:93-96`); the rows draw the line at 50 % opacity and no dot (`employee-timeline-row.tsx:529`). The probe puts the dot at `y=120` inside a header track that starts at `y=122` with `overflow-hidden` (`day-view.tsx:1555`), so the dot's top 2 px are clipped, and the track is a `translate3d` layer (`:1560`) at a fractional `left` (`use-current-time-position.ts:30-31`), so the 2 px line and the 8 px circle round to different device pixels and the dot sits visibly off the line. The header line and the row line also have different opacities, so the indicator changes colour at the header edge.
- Hard-coded colours outside tokens: `rgba(123,44,191,0.15)` row highlight (`employee-timeline-row.tsx:416`, `:450`), hatch `rgba(161,98,7,0.3)` (`work-session-block.tsx:137`; `week-view.tsx:612`), `rgba(111,95,148,0.25)` (`globals.css:284`). The existing hex and palette lint rules do not see `rgb(` literals or `<style>` strings.
- Stacking: `z-[9999]` for three floating previews, `z-[60]` panels, `z-50` popover and previews, `z-30`, `z-20`, `z-10`, `z-15`, `z-[7]`, `z-[5]`, `z-[1]`; no scale. `JobEventPopover` is a `fixed` div positioned by hand (`job-event-popover.tsx:549-565`), not a dialog primitive.
- Responsive: the day view fits 13 hours to the container by zoom (`use-timeline-zoom.ts:35-39`), so 1280 to 2560 work, but at 375 px the fixed `w-48` name column leaves about 180 px of timeline and drag-to-create is disabled on coarse pointers (`drag-to-create.tsx:58`). The week grid is the canon's named horizontal-scroll exception; the phone renders a cropped grid, not a list.

**Accessibility of drag and drop.** No view has a keyboard drag. Day-view job blocks are `div`s with pointer handlers and no focusable element (`job-block.tsx:150-176`); the work block has an inner button that opens `EntryDetailsDialog` (`work-session-block.tsx:1021-1044`), which is the form alternative for time. Job blocks reach `PlanningOccurrenceEditDialog` through the popover only when `job.occurrenceId` exists (`job-event-popover.tsx:712`); legacy jobs have no form path. Week cards are `<button draggable>` (`week-view.tsx:550`, `:746`, `:813`): openable, not movable. Parkplatz cards are non-focusable `div draggable` (`parkplatz-panel.tsx:162-164`). Resize handles are 6 px. No `aria-grabbed`, no live region for the drag itself; the Banner (`role="alert"`, `aria-live="assertive"`) announces the outcome. After a drop nothing receives focus; after a park drop the dialog takes focus and on close Radix has no source element left because the card unmounted. `CalendarEntryDialog` prevents auto-focus (`calendar-entry-dialog.tsx:494`), so drag-to-create leaves keyboard focus on a non-focusable overlay. `DispatchPanel` does manage focus (`dispatch-panel.tsx:444-455`).

**What a rewrite keeps unchanged.** `use-calendar-range-data.ts` and `lib/calendar/range-data.ts`, `/api/calendar-window` and `getCalendarWindow`, `lib/calendar/navigation.ts`, `lib/calendar/overlap.ts`, `lib/time-tracking/calendar-blocks.ts`, `calendarActionResult`, every server action, the banner adapters, and the dialogs (`EntryDetailsDialog`, `PlanningOccurrenceEditDialog`, `ParkingContextDialog`, `DispatchIssueDialog`, `CalendarEntryDialog`, `usePlanningWarningConfirmation`).

**What is tangled.** `CalendarView` and `CalendarFilters` are exported from the container and imported back by children (`week-view.tsx:25`, `fullcalendar-view.tsx:24`); Parkplatz cursor tracking for two views lives in the container (`:287-346`); `handleSessionWeekMove` and the day view's `handleCrossUserMove` duplicate the reassign logic; `snapToGrid`, `pixelToTimeStr` and `formatTimeFromPx` exist three to four times (`timeline-grid.ts:55-75`, `use-block-drag.ts:22-26`, `drag-to-create.tsx:22-30`, `work-session-block.tsx:120-130`, `job-block.tsx:43-56`); two optimistic layers. A hand-rolled `components/kalender/month-view/month-view.tsx` existed until Step 2 deleted it (Step 2 record, state to preserve); git history holds it.

**Markers to carry forward.** CL-D3 (step 3 record): the `rgb` colours and embedded style block of the month renderer. Calendar spec limitations: the browser-local timezone window, the dispatch overview's 500-occurrence cap that the typical profile already exceeds, and the FullCalendar hidden-overflow cost that is never hidden behind a later measurement start. [Realtime and caching](../../../technical/realtime-and-caching.md): do not reopen replacing FullCalendar without new measured evidence (this slice supplies that evidence, see decision Q2 below), and month readiness requires the month renderer to confirm its date. Code comments: `fullcalendar-view.tsx:484-486` capacity display "later scope (P1-11)"; `day-view.tsx:1484` untimed jobs "cannot yet be placed"; `use-calendar-range-data.ts:38` and `lib/planning/server.ts:1061-1119` carry the Step 2 `PF-*` findings.

## Direct Prerequisites And Evidence

`P1-04`, `P1-06`, `P1-08`, `P1-09`, `P1-11`, `P1-12`, `P1-14`, all accepted. Pre-Wave-3 step 3 (the clock button and the base calendar interactions) and step 5 (the beta acceptance and the 2026-09-18 production release) precede this slice; the release run's 41 groups and the cloud canary are the last full proof of the calendar's current behaviour (`2026-09-18T053234843Z-896f8bd1`, `2026-09-18T060825442Z-9804bdde`).

## Primary And Connected Feature Contracts

Primary: [calendar and resource planning](../../../features/calendar-and-resource-planning.md). Connected: employee management (schedules, absences, teams, lifecycle), jobs and projects (lifecycle, Parkplatz), service and maintenance (visit jobs as ordinary cards), time tracking (the actual-time toggle and the day view's time blocks read the shared projection; the time actions own the rules the messages explain), inventory (readiness chips through the existing readiness function, values from Wave 3). The integration table in the [step 3 record](../pre-wave-3/03-clock-fab-and-calendar-ux.md#integration-with-accepted-slices) names what the board reads from each and what it never does; it applies to the day and month views unchanged.

## Research Of 2026-09-18

Five research passes (consumer calendars, resource schedulers, German boards, technology options, optimistic UI and error UX), all web-sourced with URLs in the reports the planning session produced; the facts that shape this slice are kept here. "Unverified" marks a claim from a third party or a search snippet. Where the step 3 record already holds the 2026-09-15 findings (the 15 German products and 8 field-service boards, the Google shortcut set, the market-failure table), they are not repeated.

### Consumer calendars: what office users expect from a grid

- Google Calendar (web) ships three information densities since 2026-08 (responsive to the screen, comfortable, compact), light and dark event colour sets, sub-25-minute events at true height, "more" overflow after four or five items with a popup, and the opt-in shortcut set `t`, `j`/`k`, `d`/`w`/`m`, `c`, `e`, `z`, `/`, `g`, `?` ([Google Workspace update](https://workspaceupdates.googleblog.com/2026/07/updated-options-to-better-view-google-Calendar-on-large-monitors.html), [shortcut help](https://support.google.com/calendar/answer/37034)). Its grid keyboard model: arrows move between days, up and down between the all-day and timed sections, Tab between events, hidden events through a "More" button ([accessibility help](https://support.google.com/calendar/answer/6101541)). Third-party measurements of Google's grid: hour-row density ladder 40, 48, 60, 72, 80, 96 and 116 px per hour, a 51 px time gutter, a 14 px minimum event block, 15-minute drag snap (unverified). Its designer describes "generous white space" replacing containers and dividers and one today indicator standardized across surfaces.
- Outlook (web) offers a time scale of 5, 6, 10, 15, 30 or 60 minutes, darkens work hours, shades past days "to increase the focus on today", draws a time-indicator bar across the current day, and retired its free-form Board view in 2023 ([time scale](https://support.microsoft.com/en-us/outlook/calendar/change-the-calendar-time-scale-in-outlook), [new look](https://support.microsoft.com/en-us/office/outlook-calendar-has-a-new-look-1c04e438-d84a-44fc-a404-170c9007e65c)).
- Notion Calendar: `T` today, `J`/`K`, `1`–`9` visible days, `D`/`W`/`M`, zoom in and out of the hour grid, Shift multi-select then drag moves several events together ([help](https://www.notion.com/help/notion-calendar-settings)). Apple Calendar moves a selected event by 15 minutes with `Ctrl+Opt+arrows` and shows a red dot-plus-line now indicator; Fantastical marks availability with dot shapes, not colour alone.
- Linear's density references (the owner's benchmark for "quality interface"): 13 px small text, 4 px spacing unit, 32 to 40 px row rhythm, borders at `rgba(255,255,255,0.08)` in dark and `#d0d6e0` in light, "structure should be felt not seen", fewer separators, softened border contrast, transitions 0.1 to 0.25 s, only `transform`, `opacity` and colour animated ([Linear design refresh](https://linear.app/now/behind-the-latest-design-refresh); token values from third-party teardowns, unverified).
- Toast UI Calendar's published theme is a usable reference for hairline values: every border `1px solid #e5e5e5`, time gutter 72 px, now indicator `#515ce6` with the past segment dashed and today's solid, today tint `rgba(81,92,230,0.05)`, past time `#bbb` ([theme docs](https://github.com/nhn/tui.calendar/blob/main/docs/en/apis/theme.md)).

### Resource schedulers and dispatch boards

- Dynamics 365 Field Service: a drop keeps the booking's duration; `Ctrl`-click multi-select then "Move by" (offset) and "Reassign to"; row height, time resolution, working time and "show bookings proportional to duration" are board settings; the 2024 React rewrite added a current-time "dayline" and cut load time by 30 to 68 %; Microsoft's guidance is 25 to 50 resources per board tab ([drag and drop](https://learn.microsoft.com/en-us/dynamics365/field-service/schedule-board-drag-drop), [settings](https://learn.microsoft.com/en-us/dynamics365/field-service/schedule-board-tab-settings)).
- ServiceTitan's dispatch board: technician rows, horizontal or vertical toggle, "Compact" and "Expanded" density, one colour per status (scheduled, confirmed, working, dispatched, paused, completed), a jobs tray with Unassigned, Hold and Alerts tabs, "click-to-drop" as the non-drag alternative, an arrival-window prompt on drop ([help](https://help.servicetitan.com/residential-s-r/docs/use-the-new-daily-and-weekly-dispatch-board)).
- Jobber: day view in vertical (team columns, Unassigned first) or horizontal (names left, bars over time) orientation, overlapping card layout for density, business hours white and off-hours muted, weekends hideable ([help](https://help.getjobber.com/hc/en-us/articles/29840886387351-New-Schedule)). Housecall Pro caps its employee view at 15 rows and prompts for customer notification on drag.
- Odoo Planning: published shifts full colour, unpublished diagonal stripes, time-off rows greyed, split by scissors, day precision quarter, half or full hour ([docs](https://www.odoo.com/documentation/18.0/applications/services/planning.html)).
- Component defaults worth adopting as starting values: Bryntum `rowHeight` 60, `barMargin` 10, 15-minute `timeResolution`, sticky event labels, current-time line off by default; Mobiscroll `dragTimeStep` 15, virtual scroll on, "more" button above `maxEventStack`; Syncfusion `rowAutoHeight` grows rows by overlap count; FullCalendar resource timeline is Premium (from 480 USD a year) and was never part of this app.

### German boards (beyond the 2026-09-15 table)

- HERO's board has rows for employees, vehicles, machines, tools and rooms, hides weekends, shows the year and the current week at once, warns on overlaps, absences and holidays before saving, and is desktop-only; reviewers praise "ich sehe auf einen Blick, wo meine Mitarbeiter sind" and report "Aktualisierungsverzögerungen bei größeren Daten" ([HERO](https://hero-software.de/features/baustellenplaner/digitale-plantafel), [reviews](https://www.softwareabc24.de/handwerker-software/hero--die-handwerkersoftware)).
- plancraft: projects in the left column, employees as rows, a deployment copied by dragging between rows, "Verschieben, verlängern, verkürzen – alles geschieht in einem Schritt"; "bei sehr großen und komplexen Projekten wird das System manchmal unübersichtlich" ([help](https://help.plancraft.com/de/articles/382799-plantafel-fur-projekt-einsatz-und-abwesenheitsplanung), [OMR](https://omr.com/en/reviews/product/plancraft)).
- Meisterwerk: "Überbuchungsschutz", hover preview on cards, neutral holding positions for pre-planning before a person is assigned, regional holidays ([Meisterwerk](https://www.meisterwerk.app/einsatzplanung)). Craftnote warns on double booking and auto-saves. TAIFUN rotates the axes ("Zeitleiste": employees left, time on top), has zoom buttons and variable row heights, and a dispatcher view. openHandwerk zooms in 8-hour steps, multi-selects with Shift, and lists Mitarbeiter, Autos, Zubehör, Spezialwerkzeug and Subunternehmen as resource types.
- Sykasoft (the beta customer's current tool): the board window scales for a second monitor; clicking a Monteur's name isolates his row; the card header is configurable (customer number, job number, description); "Ansicht drucken" prints the board as displayed; Arbeitsmittel disappear when booked concurrently; an Einsatzliste per day or week is filtered by type, person and Fahrtzone ([manual](https://q.sykasoft.de/wp-content/uploads/2021/06/einsatzplaner-handbuch.pdf)). These are the habits Willert's office brings; the second-monitor case is the read-only mode (criterion 17), the row isolation is a one-click filter (criterion 10), the card header is criterion 4.
- Labelwin colours appointments in the same Planquadrat purple so nearby colleagues stand out; "Den Kalender haben wir alle am Schreibtisch geöffnet" is the vendor's customer quote for a board that lives on every office screen all day.
- STREIT V.1 is the cautionary tale: "Die Geschwindigkeit des Programmes und der damit verbundene Workflow ist unterirdisch schlecht" (ProvenExpert, 2023).

### Optimistic UI, Undo and refusal messages

- React 19's `useOptimistic` reverts silently when the action throws; the message is the app's job. TanStack's pattern (snapshot in `onMutate`, roll back in `onError`, refetch in `onSettled`) is what the range owner already does through `beginMutation` and the settle read ([react.dev](https://react.dev/reference/react/useOptimistic), [TanStack](https://tanstack.com/query/latest/docs/framework/react/guides/optimistic-updates)). Mishunov's rule: optimistic only where success is the 97 to 99 % case, and failure must be visible within two seconds ([Smashing](https://www.smashingmagazine.com/2016/11/true-lies-of-optimistic-user-interfaces/)). Linear applies the change in memory before saving and rolls back a rejected transaction while keeping previous values for undo; a failure shows a small toast naming the thing that failed (third-party write-up, unverified).
- Google Calendar confirms a drag with a snackbar and Undo and lets `z` undo the last change; Gmail's undo window is 5 to 30 seconds. Raskin: "Never use a warning when you mean undo." Nielsen: confirm only actions with serious consequences, "go to great lengths to provide undo" ([NN/g](https://www.nngroup.com/articles/confirmation-dialog/)).
- Refusal wording that works: Outlook's InfoBar "Konflikte mit einem anderen Termin" and "This appointment occurs in the past"; Google's "You don't have permission to edit this event". NN/g's guidelines: close to the source, human language, precise description, constructive advice, no blame, preserve the input ([NN/g](https://www.nngroup.com/articles/error-message-guidelines/)). GOV.UK: describe what happened and how to fix it; never "please", "sorry", "invalid", "oops", "forbidden" ([GOV.UK](https://design-system.service.gov.uk/components/error-message/)). Fluent: a toast without an action times out after seven seconds; never use toasts for necessary actions; Atlassian: never auto-dismiss an error flag.

### Technology options (state on 2026-09-18)

- FullCalendar: the app runs v6.1.19; v7 shipped on 2026-06-19 (React connector without the internal Preact layer, flexbox instead of nested tables, a class-name theme system, `temporal-polyfill` as a mandatory peer, premium licence moved to AGPLv3) ([upgrade guide](https://fullcalendar.io/docs/upgrading-from-v6)). Open since 2024 and still open in v7: event content is unmounted and remounted on every render because segment keys change (#7650), `eventContent` is called when nothing changed (#5594), no virtualization outside the premium timeline (#5673). The resource timeline (people rows) has always been premium (from 480 USD per developer per year) and was never part of this app. Verdict: v7 would fix the table DOM and the theming, not the remounts, the hidden overflow mounts or the third drag mechanism.
- Alternatives with a people-row timeline are commercial (Bryntum from 680 USD per developer, Mobiscroll from 995 USD per project, DHTMLX from 599 USD, Syncfusion community licence only under 1 M USD revenue and five developers, Schedule-X resource scheduler and even its drag-and-drop at 479 EUR per year); react-big-calendar (MIT, 52 kB gzip, resources as columns only, a mandatory date localizer) and Toast UI (archived 2026-09-02) do not fit. Nothing in this list gives the app control over hairlines, card anatomy, optimistic drops and one engine across three views; a hand-rolled grid on CSS grid does, and the month grid is the smallest of the three. Headless helpers worth reading, not adopting: `@h6s/calendar` (grid math, 6 kB), `lramos33/big-calendar` (a shadcn calendar with custom drag and drop).
- Drag and drop: Atlassian's pragmatic drag and drop (Apache-2.0, about 5 kB) wraps the browser's native HTML5 drag events, which is exactly the mechanism behind today's week-view feel (`dragover` storms, a browser-owned ghost, unreliable touch, no keyboard) and its own guide says to provide non-drag alternatives through menus. `@dnd-kit/core` has not been released since 2024-12; the `@dnd-kit/react` rewrite is 0.5 with an unanswered maintenance question (#1830). React Aria's `useDrag` and `useDrop` (Apache-2.0) bring a keyboard and screen-reader model but are built for lists of drop targets and would add a second interaction stack beside Radix. Native pointer events with `setPointerCapture` (Baseline 2020), `touch-action: none` on the handle to stop `pointercancel`, coalesced move samples, and one `requestAnimationFrame` flush per frame need about three hundred lines and no dependency ([MDN](https://developer.mozilla.org/en-US/docs/Web/API/Element/setPointerCapture)).
- Rendering techniques: `transform` and `opacity` only for the ghost (web.dev animation guide); `will-change` toggled from script during the drag, never static; `content-visibility: auto` with `contain-intrinsic-size` for off-screen rows (Baseline since 2025-09), with the sticky axes kept outside any contained ancestor because `contain: paint` clips them; `@tanstack/react-virtual` (MIT, 7.6 kB gzip) for boards above roughly fifty rows, which Dynamics' own guidance calls the practical limit per tab; React 19.3's `<ViewTransition>` for view switches under `startTransition`; the React Compiler is stable and Next.js 16 enables it with `reactCompiler: true` and an `annotation` mode for opt-in files, which the implementing agent measures on `components/kalender` before relying on it.
- Measurement: the React `<Profiler>` counts commits per drag in the component contract; Long Animation Frames (`long-animation-frame`, 50 ms threshold, Chrome 123+) attribute jank in the audit run; both go into the evidence, LoAF marked Chrome-only.

### Principles distilled for this slice

1. Structure is felt, not seen: one hairline token at roughly 8 to 12 % foreground alpha for every grid line, no cell cards, no gutters between cells, no doubled borders (Linear, Toast UI, Google's white-space rule).
2. Today and now are the two loud things in a grid: a subtle today tint of about 5 % plus a strong date marker, and one now indicator drawn identically in every view (Google, Outlook, Toast UI).
3. Text in a card is never below 11 px; absence bars, holiday bars and chips meet 4.5:1 against their fill in both themes (WCAG 1.4.3; the light-theme absence bars fail today).
4. A drop keeps the duration and the time of day; only an explicit edge drag changes length (Dynamics, criterion 22).
5. Snap to 15 minutes by default, 5 minutes with a modifier; the ghost follows the pointer at display refresh through `transform`, never through layout (Bryntum, Mobiscroll, Linear's animation rule).
6. Every move is optimistic, confirmed by a banner with Undo after persistence, reverted with a specific sentence on failure; nothing asks "Wirklich verschieben?" for a reversible move (Raskin, Nielsen, Google).
7. A refusal names the rule and the next step: "Sven Neumann ist am 18.9. im Urlaub. Wähle einen anderen Tag oder eine andere Person." beats "Auftrag konnte nicht verschoben werden." (NN/g, GOV.UK).
8. Density is the user's choice: two modes remembered per user, rows that grow with their content, the day-view zoom ladder (Google 2026, ServiceTitan, Syncfusion, TAIFUN).
9. The board uses the whole screen: at 2560 px the columns and cards grow, at 1280 px seven days still fit without a horizontal scroll, and horizontal scrolling appears only with a longer horizon (Google "responsive to your screen"; Sykasoft's second-monitor habit).
10. Every drag has a non-drag path: a focusable card, Enter for the edit dialog, "Verschieben nach …" and "Zuweisen an …" commands, click-to-drop (WCAG 2.2 2.5.7; Dynamics; ServiceTitan).
11. Colour supplements, never carries: dispatch state and absence kind show an icon or a label beside the tint (Fantastical's dot shapes, the calendar spec's role and UX principles).
12. Overflow is honest: "+n mehr" opens the full day; nothing is mounted hidden (Google, Mobiscroll `maxEventStack`).
13. Weekends are visible and shaded by default and can be hidden per user (HERO, Jobber, Google's "show weekends").
14. Whatever the analog board did in one motion stays one motion: notes in two clicks, a copy by drag with a modifier, a row isolated by clicking the name (Craftboxx, plancraft, Sykasoft).
15. Speed is a feature the user can feel in the first frame: the measured budgets below cover the drag, the drop and the resize, not only the navigation.

## Design Direction

The target that "bold" points at, per surface. The implementing agent renders these in both themes at 1280×720, 1440×900, 1920×1080, 2560×1440, tablet landscape and 375 px before the pre-implementation report and adjusts them with the owner.

### One calendar design language

- Tokens in `app/globals.css`, consumed by every view and panel, none of them hard-coded: `--calendar-grid` (hairline, roughly 8 to 12 % of the foreground), `--calendar-grid-strong` (hour lines and column dividers, about twice that), `--calendar-cell-off` (weekends, holidays, closures, non-working time), `--calendar-today` (about 5 % tint), `--calendar-now` (the indicator), `--calendar-gutter` (the time axis and the name column background), plus the planning, actual-time, absence, holiday and note card families derived from the existing purple, neutral and semantic tokens. A lint rule rejects `rgb(`, `rgba(`, `hsl(` and hex literals in product JSX and in `<style>` strings under `components/kalender` (the existing hex and palette rules miss them), so the calendar cannot regress to literal colours (enforcement ladder tier 2).
- One grid renderer: CSS grid or absolute positioning inside one scroll region per view, grid lines painted by the container (`repeating-linear-gradient` or one border per track), never one DOM node per line; `border-collapse` semantics without a table; the container radius is 8 px (`rounded-lg`) with the header and body inside one clipped region so no border is cut; scroll regions carry `scrollbar-gutter: stable` on the body only, and the header never has a scroller of its own.
- One now indicator: a 2 px line in `--calendar-now` with a 8 px dot centred on the line at the axis edge, drawn once per view from one hook, positioned on whole device pixels (`Math.round` of the pixel offset, no fractional `left` inside a transformed layer), never clipped by a header track, one colour and opacity from top to bottom.
- One card anatomy: title, then customer and place, then a chip row (dispatch state, readiness, series mark, commitment mismatch, provisional time); the same component in the board, the day view and the month view with three size variants; sticky labels inside long bars (Bryntum); `text-xs` minimum.
- One drag engine for every draggable (card, bar edge, time block, Parkplatz card, untimed chip, drag-to-create): pointer events with `setPointerCapture`, a ghost moved by `transform` inside `requestAnimationFrame`, hit testing against a precomputed slot map (no `getBoundingClientRect` per move), no React state during the drag except one throttled hover-target update per frame at most, auto-scroll near the edges, 15-minute snapping, modifier for fine snapping and for copy, touch with a long-press start, Escape cancels, a drop announcement in the live region. Decision Q6 picks the implementation (in-house engine or a library).
- One optimistic owner: every drop writes its intended result into the range owner under `beginMutation` before any server call (the day view's private override maps go away), the card is confirmed with Undo after persistence, rolled back with the specific message on failure, and no action response triggers a page render (`revalidatePath('/kalender')` leaves the planning, dispatch, work-lifecycle and time actions or is scoped so the calendar's own reads own freshness). Unpark becomes one server action. A park drop moves the card first and opens the context dialog second; cancelling the dialog rolls the card back.
- One message layer: `lib/calendar/messages.ts` (name to be confirmed) holds an exhaustive typed map from every calendar action's result code to a German sentence that names the rule and the next step, with placeholders for the person, the date and the time where the code carries them; the type is `Record<CalendarActionErrorCode, …>` over a union built from the action result types, so a new code without a sentence fails `tsc` (tier 1), and a unit test checks that no calendar component renders a raw code (tier 2). Pre-checks that the client can decide (overlap, future time, non-working day, absence, read-only mode, employee without login, internal entries cannot be parked) refuse before the drop with the same sentence, shown as a red preview and an inline message at the pointer, as the overlap preview does today.
- Density: "Kompakt" and "Komfortabel" remembered per user and organization; rows grow with their content (overlap count) from a minimum; the day view keeps its zoom ladder and gains keyboard zoom.
- Dark mode: every token has a dark value; the rendered review covers both themes for every state (today, now, absences, holidays, closures, provisional time, selection, drag ghost, drop target, refused drop).

### The Plantafel (week view)

As specified on 2026-09-15 (criteria 1 to 22 below), rendered in the language above: seven columns fit at 1280 px with a 160 px sticky name column and two-line cards; horizons of two to six weeks scroll horizontally inside the region with the name column sticky; the today column tinted with the date marked; weekends shaded and hideable; holidays and closures shading whole columns with their label in the header; non-working days per person shaded in the row; absences as bars across their dates; capacity as a quiet per-cell state (free, partly planned, full, overbooked) with numbers on hover and in a keyboard-reachable tooltip; team headers collapsible; an "Ohne Zuweisung" row; cards with the dispatch chip; multi-assign cards linked on hover; notes as distinct cards; the read-only switch; filters, search and "nur Konflikte"; `t`, `j`, `k`, `z`, `?`; the actual-time toggle. The board is the manager's home view when a manager opens `/kalender` without a bookmark.

### The day view

The hour grid for precise dispatch: rows per person with content-driven height, the time axis sticky, the name column sticky, 15-minute snap, drag-to-create, edge resize with a visible 8 px handle and a 24×24 hit area, overlap laid out side by side (Jobber's overlapping layout) instead of stacked z-indexes, the now indicator across all rows, working time shaded per person from the schedule, travel gaps marked, untimed jobs in a per-row tray that can be dropped onto the grid, the same card anatomy and the same drag engine as the board. At 375 px the name column collapses to avatars and the grid keeps at least 70 % of the width.

### The month view

The overview: six weeks, cells that grow with the window (no fixed 120 px), holidays, closures and absences as bars, per-day counts, "+n mehr" opening the day in place, drag to move a visit to another day with the same engine, the today cell marked, past days slightly dimmed (Outlook). Decision Q2 (decided 2026-09-18) makes this a rewrite on the shared layer; the five FullCalendar packages leave with it. The month view stops mounting hidden overflow events, has no doubled borders, no clipped corners and no header scroller, and reports its readiness date as the freshness contract requires.

### Panels, popovers and dialogs

The Einsätze panel and the Parkplatz become side panels on the same scale and tokens; the Parkplatz cards are focusable and use the shared engine; the job popover moves onto the `Popover` primitive with focus management; a z-index scale replaces the ad-hoc values; every dialog opened from a drop returns focus to the card that caused it (or its successor after a move).

## In Scope

Work packages, in the order the implementation should take them (each is a reviewable step with its own gates; the slice is accepted as a whole):

- **A. Foundation.** The calendar tokens and the literal-colour lint rule; the grid renderer; the drag engine; the optimistic owner (day-view overrides merged into the range owner, park-then-dialog, unpark as one action, `revalidatePath` removed or scoped); the message layer with the typed exhaustive map and its unit check; the focus and live-region rules; the z-index scale; the measured interaction scenarios registered (see coverage).
- **B. The Plantafel.** Replace the week view's rendering with the board inside the same `CalendarView` value and tab position: horizon of 1, 2, 4 or 6 weeks; today marker; Monday start; shaded weekends (hideable), holidays, closure days and per-person non-working days; rows for active employees grouped by team, an "Ohne Zuweisung" row; row kind as a union with `person` as the only member (prepared, not built: `P1-33` ends a row at the offboarding date through the same P1-24 lifecycle read; `P1-46` may later offer „Kunden informieren" after a move as an explicit opt-in follow-up, never automatically; Phase 2 planning proposals render as ghost cards that a manager accepts or rejects on the same drop path); cards for timed visits, all-day and multi-day bars, series marks, skipped and cancelled states, dispatch state chip, readiness chips from the shared readiness function (the card never computes readiness itself, so `P1-26`, `P1-27`, `P1-32` and `P1-50` change values, never the board); absences in the row; per-cell capacity from one window-level capacity read beside the window GET; drops: reassign, move date, extend all-day bars by edge drag, park, unpark, multi-assign, copy with a modifier, with the existing qualification, capacity, supersession, commitment and series dialogs; keyboard and form alternatives; `t`, `j`, `k`, `z` and a `?` overlay; notes as all-day `internal` occurrences of kind `Sonstiges`; read-only mode; filters, search and "nur Konflikte"; per-user persistence; the actual-time toggle; tablet landscape usability; the employee's own row as a list at phone width.
- **C. The day view** on the foundation, as described above.
- **D. The month view** on the foundation, per decision Q2.
- **E. Panels and dialogs** on the shared scale and primitives; every dialog and form maps its codes through the message layer.
- **F. Messages and pre-checks** for every calendar action: move, resize, reassign, park, unpark, create, drag-to-create, extend or shorten time entries and work sessions, dispatch, series edits, commitments, Undo. The sentence per code is proposed to the owner in the pre-implementation report as a table (code, situation, sentence, next step).
- **G. Performance.** Zero React commits per pointer move; drop-to-visible-move within one frame; the DOM per view recorded; the registered scenarios measured on the candidate build at 1280×720 and 1920×1080 on the typical profile with reviewed budgets; the six-week board scenario.
- **H. Accessibility and responsiveness.** WCAG 2.2 2.5.7 (a non-drag alternative for every drag), 2.4.11 (focus never obscured by the sticky axes or panels), 1.4.11 (3:1 for control boundaries), 2.5.8 (24×24 targets), 1.4.3 (4.5:1 text); rendered review at every viewport and both themes; the phone layouts.
- **I. Documentation, catalog and enforcement.** The calendar spec baseline and limitations, the design skill's calendar section (tokens, card anatomy, drag engine, message rules), the freshness contract's month-readiness sentence and the Step 2 note, the user-flow catalog (`P1-24a-F01` onward plus the repointed base and A1 clauses), the roadmap, this record, and a decision record amendment if Q2 replaces FullCalendar.

## Explicit Non-Goals

- Vehicle, tool or material rows (`P1-28`, `P1-32`; the row-kind union is the seam).
- Material or tool readiness values (`P1-26`, `P1-27`, `P1-32`); the chips show "nicht reserviert" and "nicht bewertet".
- Route or map view, travel-time estimates (`P1-50`); external calendar subscription or sync.
- Customer messages after a move (`P1-46`), automatic proposals (Phase 2).
- A free-form whiteboard canvas; project-level Gantt bars; a phone layout of the whole board (`P1-49` gives the phone its own row view); printing the board (Sykasoft prints; the spec allows print only as a real operational fallback, and no customer has asked).
- Any new table, enum, Realtime subscription, message, reservation or order; any change to the rules the actions enforce (the messages explain the rules, they do not move them).
- Hiding a rendering cost behind a later measurement start or a raised budget: the month view's completeness, its overflow counts and its interactions stay measured in place.

## Product Decisions Required Before Coding

Decided by the owner on 2026-09-15 (recorded in the step 3 record): the board is the week view, labelled "Plantafel" for managers and "Woche" for employees; notes reuse the `Sonstiges` internal kind (a lighter note kind only if a customer asks); the read-only mode is in scope; the slice ID and placement are `P1-24a` before `P1-25`.

Decided by the owner on 2026-09-18: the day view, the month view, the panels and every calendar message belong to this slice; the goal is the best calendar experience the product can offer, not a polish pass; the implementing agent is expected to find and propose improvements this plan does not list; performance is fixed at the level of the rendering and interaction architecture, not by tuning.

Put to the owner on 2026-09-18 with recommendations; the owner accepted every recommendation the same day, so each item below is decided as recommended:

- **Q1 Slice shape.** One slice for A to I, or `P1-24a` (foundation and board) followed by `P1-24b` (day, month, messages). Recommendation: one slice, delivered as the work packages above with a gate after A and B; the implementing agent may propose the split in its pre-implementation report if the review load says so, and the roadmap keeps the outcome either way.
- **Q2 Month view.** Rewrite the month view on the shared layer and remove the five FullCalendar packages, superseding the Step 2 note that rejected the replacement "without new measured evidence" (the evidence is this audit: doubled borders, clipped corners, the header scroller, 72 literal colours, hidden overflow mounts, a snap-back before every drop, a 740-line style block, a third drag mechanism), or keep FullCalendar and restyle it through its CSS variables. Recommendation: rewrite; the month view is the simplest of the three grids, the deleted hand-rolled month view is in git history as a starting point, and one engine across three views is the point of the slice. Recorded as an amendment to the freshness contract and a note in the Step 2 record's durable homes.
- **Q3 The now indicator's colour.** A dedicated `--calendar-now` token in the red family (Google, Apple, Outlook's convention; never confused with the orange action colour) or the orange primary (the month view today). Recommendation: red family, documented in the design skill as the one non-status use of that hue, identical in all three views.
- **Q4 Density modes.** Two densities ("Kompakt", "Komfortabel") remembered per user, plus content-driven row heights. Recommendation: yes, default "Komfortabel"; the office switches to "Kompakt" on a laptop.
- **Q5 The board at laptop width.** Seven days fit at 1280 px without a horizontal scroll (160 px name column, two-line cards) and only longer horizons scroll sideways, or keep the sideways-scroll exception for one week too. Recommendation: fit seven days; the canon exception then applies only to horizons above one week and to the day view's hour axis.
- **Q6 Drag engine.** In-house pointer-event engine or a library (pragmatic drag and drop, `@dnd-kit/react`, React Aria; facts in the technology section). Recommendation: in-house, about three hundred lines in `components/kalender/drag-engine/` (`setPointerCapture`, `touch-action: none`, a precomputed slot map for hit testing, a `transform`-only ghost flushed once per animation frame, auto-scroll, Escape, a long-press start on touch), because the native HTML5 drag events that pragmatic drag and drop wraps are the cause of today's week-view feel, `@dnd-kit`'s rewrite is 0.x with an open maintenance question, and React Aria would add a second interaction stack beside Radix. The keyboard path is a command menu on the focused card ("Verschieben nach …", "Zuweisen an …", "Parken"), not a simulated keyboard drag.
- **Q7 Month drag.** Keep drag-to-move-a-visit in the month view (Google does; the base flow `BASE-CALENDAR-F02` promises it). Recommendation: yes, same engine, same dialogs.
- **Q8 Weekends.** A per-user "Wochenende ausblenden" toggle on the board and the month. Recommendation: yes; cheap and expected (HERO, Jobber, Google).
- **Q9 Day-view rows.** Content-driven row height with side-by-side overlap layout instead of fixed 110 px rows and stacked z-indexes. Recommendation: yes.
- **Q10 Copy by drag.** `Alt`-drag copies a visit to another person or day (plancraft's "copy deployment", Bryntum's `copyKey`). Recommendation: yes for the board, through the existing create action with the same dialogs.

Decided by the owner on 2026-09-18 as a judgment call recorded here, not asked: managers land on the board when they open `/kalender` without a bookmark and employees on the day view, and the last chosen view is remembered per user after the first switch (the owner can overrule this in one line before the slice starts).

The visual design of cards, capacity colours and tokens follows the `werkflow-design` skill (purple for planning entities, semantic status colours, orange only for the selected card and the primary action) and is rendered for the owner before implementation.

## Data Ownership And Historical Semantics

The calendar owns no data. Occurrences, assignments, dispatches, absences, schedules, capacity assessments and time projections keep their owners; every calendar action calls the owning action and the owner's history rules apply unchanged. Per-user calendar preferences (horizon, density, weekends, filters, read-only, actual-time toggle) are stored with the existing organization-scoped user preferences.

## Permissions And Organization Isolation

Reads go through the existing window GET (organization-scoped, role-filtered) plus one capacity read with the same authorization. Employees receive only their own occurrences and never another person's capacity or absence. Every drop calls an existing manager-only action that re-authorizes. The capacity read is proven to deny outsiders and to exclude other members' data for employees. An optimistic move never shows an employee a state they are not allowed to see.

## UI And Field-Worker Behavior

Employees keep `Mein Einsatz` and see the board only as their own row with no drag; their day and month views keep today's own-data rule. Managers get the dense board and day view. Every drag has a keyboard and form path. Colours supplement labels and icons and never carry status alone. Touch targets on the field paths stay at 44 px.

## Realtime, Caching, Offline, And Failure Recovery

The calendar consumes the range owner: retained grid, stale and error states, mutation ownership, Undo after persistence and rollback with the error at the point of action apply unchanged and now hold in every view. Another session's change reaches every view within `LIVE_TARGET_MS`. A drop's optimistic state survives a Realtime echo and is replaced only by the settle read. No offline queue. No action response renders the route.

## Migration And Rollback

No schema change. Rollback is the previous rendering (git history); preferences left behind are harmless. If Q2 removes FullCalendar, the five packages leave `package.json` and `bun.lock` in the same change and `knip` proves nothing else imports them.

## User Flows (Catalog IDs)

`P1-24a-F01` onward, proposed to the owner before implementation as the protocol requires, covering: opening at each horizon; today marker and navigation; team grouping and collapse; person rows and the "Ohne Zuweisung" row; timed cards, all-day, multi-day, series, skipped and cancelled cards; absences, holidays, closures and non-working days; capacity states and their explanation; every drop kind with its warnings and dialogs; keyboard and form alternatives; Undo and rollback; notes; read-only mode; filters and search; persistence; density and weekend toggles; the actual-time toggle; freshness; employee visibility; organization isolation; the no-side-effect clauses. The day and month flows: drag-to-create, move, resize, overlap layout, untimed tray, the now indicator, month drag, "+n mehr", past-day dimming, the refusal sentences per rule. The clauses that drive today's views (`BASE-CALENDAR-F01` to `F04`, the wave-1 audit's `A1-21`, `A1-24` and `A1-25` derivations, the P1-11 and P1-12 calendar clauses) are repointed in the same change; none may lose coverage.

## Acceptance Criteria

Criteria 1 to 22 were drafted in the step 3 record on 2026-09-15 and are copied here verbatim on 2026-09-18; 23 onward are the 2026-09-18 extension.

1. The board is the week view: the "Woche" tab position renders it for every role, labelled "Plantafel" for `admin` and `buero` and "Woche" for `employee`, whose board has exactly their own row.
2. Rows are active employees, grouped by P1-09 team with a collapsible team header, ordered by team then name; people without a login appear (they can be planned). An "Ohne Zuweisung" row at the top holds occurrences without assignees.
3. Columns are Berlin dates; the horizon is 1, 2, 4 or 6 weeks, chosen in the header and remembered per user; navigation by week, "Heute" scrolls the current date into view and marks it; weekends are visible but shaded; holidays and closure days shade the whole column with their label.
4. A card is one P1-11 occurrence: timed visits show start time and duration, all-day and multi-day occurrences render as one bar across their dates; cards show title, customer, place, job number and the P1-12 dispatch state (nicht gesendet, gesendet, bestätigt, Rückfrage, nicht möglich) as a chip; skipped and cancelled occurrences render muted and read-only; series occurrences carry the series mark.
5. Absences render in the row: approved vacation as "Urlaub", pending as dashed "angefragt", sickness as "Abwesend", spanning their dates; a person's row shows their schedule's non-working days shaded from P1-04.
6. Each person-day cell shows its capacity state from the P1-11 assessment for the visible window: free, partially planned, full, overbooked, with the planned and available minutes on hover and in a keyboard-reachable tooltip; the read is one window-level capacity call beside the existing window GET, never per cell.
7. Drag-and-drop: a card dragged to another row reassigns (same rules and confirmation dialogs as the day view: qualification, capacity, dispatch supersession); dragged to another column moves the date; the horizontal edge of an all-day bar extends or shortens it; dragged onto the Parkplatz parks it; a Parkplatz card dropped on a cell unparks it to that person and date. Every drop is optimistic with the calendar's `beginMutation` ownership, confirmed by a success banner with Undo after persistence, and rolled back on failure with the error at the point of action.
8. Keyboard and form alternative for every drag: a card is focusable; Enter opens its edit dialog where date and assignees change; the existing planning dialogs are the form path.
9. Multi-assign: an occurrence with several assignees shows one card per assignee row, linked visually on hover; a drop on another row of a linked card adds or moves that assignment as the day view does today.
10. Filters: team, employee, job, customer, dispatch state and "nur Konflikte"; the member filter of the calendar applies; a free-text search matches title, customer and job number.
11. Freshness: the board reads through the range owner; another session's move, park, dispatch or absence change appears within `LIVE_TARGET_MS`; the retained-grid, stale and error states of the window contract apply unchanged.
12. Performance: opening the board for a six-week window of the typical profile is a registered measured scenario with a reviewed budget; the window read stays one GET plus one capacity GET.
13. Readiness chips: material "nicht reserviert" and tools "nicht bewertet" on every card, from the same readiness function the Einsätze panel uses, so the Wave 3 slices only change the values.
14. Employees never see the board; their occurrence visibility and acknowledgement flows are unchanged; organization isolation is proven for the capacity read.
15. No new table, enum or Realtime subscription; no message, reservation or order is caused by a board action.
16. Notes: an office user can add a note to a day or to a person-day from the board in two clicks; it renders as a distinct card (no capacity effect, no dispatch) and is an all-day `internal` occurrence of kind `Sonstiges` unless the owner chooses a lighter note kind (decision 14).
17. A read-only mode ("Nur ansehen") switch in the board header disables every drag until switched back; it is per user and remembered, so a second monitor in the office can show the board without accidental moves.
18. Keyboard: `t` today, `j` / `k` next and previous window, `z` undo of the last board move while its Undo banner is visible; the shortcuts are listed in a `?` overlay and never fire inside an input or dialog.
19. Layout: the grid scrolls sideways inside its own region with a visible edge (the existing canon exception), the name column stays sticky, and the board is usable on a tablet in landscape; on a phone width the employee's own row renders as a list, never a cropped grid.
20. Persistence: horizon, filters, search, the read-only switch and the actual-time toggle are remembered per user and organization; scroll position and the selected card survive a save, a Realtime update and a manual refresh.
21. Actual time: the "Ist-Zeiten anzeigen" toggle renders recorded time blocks and provisional correction badges in the row from the shared projection, defaulting on for the one-week horizon and off otherwise.
22. Drops on dispatched or committed occurrences show the existing supersession and commitment-mismatch dialogs before applying; a drop keeps the occurrence's duration and time of day; a series occurrence asks "nur dieser" or "diese und zukünftige" as the day view does.
23. One design language: every grid line, cell background, today tint, now indicator and card colour in all three views, the Parkplatz and the Einsätze panel comes from the calendar tokens in `app/globals.css`; a lint rule rejects colour literals under `components/kalender`; the rendered review finds no doubled border, no clipped corner, no scroller inside a header, no nested page scroll, and the same hairline weight in every view.
24. The now indicator is one component with one token, one width and one opacity from the axis to the last row, its dot centred on its line on whole device pixels and never clipped, present in the day view, the board's today column and the month's today cell.
25. Text contrast: every card, bar, chip and axis label meets 4.5:1 against its fill in both themes; no calendar text below 11 px; the absence and holiday bars are legible in the light theme.
26. Every drop in every view is optimistic before the first server call: the card is at its target within one frame of the release, the park drop moves the card before the context dialog opens (and cancelling the dialog rolls it back), the month drop never snaps back before it moves, unpark is one server call, and no action response triggers a route render.
27. Every refusal the calendar can show names the rule and the next step in German through the typed message map; no component renders a raw code; the map is exhaustive over the union of calendar action result codes (a missing sentence fails `tsc`); the client pre-checks it can decide refuse before the drop with the same sentence; Undo failures name their cause.
28. Drag performance: the ghost follows the pointer by `transform` inside `requestAnimationFrame` with no React commit per pointer move (a component contract counts renders during a synthetic drag); grid lines are painted by the container, not by per-line elements; the DOM element count per view on the typical profile is recorded in the slice evidence.
29. Registered measured scenarios cover drop-to-visible-move and drop-to-settled for a board reassign, a day-view resize and a month-view move, plus the six-week board open and the existing six navigation scenarios, with reviewed budgets on the typical profile at 1280×720 and 1920×1080.
30. The day view: content-driven row heights with side-by-side overlap layout, 15-minute snap, an 8 px resize handle with a 24×24 hit area, drag-to-create, a per-row tray for untimed jobs, working time and travel gaps shaded, the leftmost hour label never cut; at 375 px the timeline keeps at least 70 % of the width.
31. The month view: cells grow with the window, "+n mehr" opens the full day in place, nothing is mounted hidden, past days are dimmed, a visit can be moved by drag with the same dialogs, and the view reports its readiness date to the range owner as the freshness contract requires.
32. Responsiveness: no horizontal scroll at 1280×720 for the one-week board, the day view and the month view; the board and the day view use the full width at 2560×1440 (columns and cards grow); density "Kompakt" and "Komfortabel" and the weekend toggle are remembered per user.
33. Accessibility: every drag has a keyboard command path ("Verschieben nach …", "Zuweisen an …") and a form path; focus returns to the moved card after a drop and after every dialog opened by a drop; the live region announces the drop result; the sticky axes never obscure a focused card (WCAG 2.2 2.5.7, 2.4.11, 2.5.8).
34. Dark mode parity: every state in criterion 23 to 31 is reviewed in both themes; no `.dark` override is a literal colour.

## Clause Coverage And Executable Groups

`golden:p1-24a` (one journey across scheduling, dispatch, a park and unpark, a cross-session move and a refused drop with its sentence), an exhaustive audit spec registered like the Wave 2 ones, `ui:contracts` contracts for the board, the day view and the month view (drag engine render count, optimistic state and rollback, keyboard path, read-only mode, focus return, message map) against the real range owner, a `@FRESHNESS` stage, and the registered measured scenarios of criterion 29 (`navigation-to-usable-content` for the six-week window, `interaction-to-visible-change` and `interaction-to-settled` for the three drops). The existing `tests/ui-contracts/calendar.spec.ts`, `day-view.spec.ts`, `month-view.spec.ts` and their boundaries are replaced or extended, never deleted without a successor that covers their clauses.

## Automated And Manual Verification

Per [decision 0007](../../../decisions/0007-independent-test-groups.md): the static groups, `unit:all`, the groups above, the affected A1 and base calendar clauses, and `audit:performance:calendar-live`. Rendered review in both themes at 1280×720, 1440×900, 1920×1080, 2560×1440, tablet landscape and 375 px, for every state named in the design direction, with screenshots attached to the record. A React Profiler and Long Animation Frames capture of a drag before and after, recorded in the evidence.

## Documentation Updates

Calendar spec baseline, limitations and open decisions; user-flow catalog (new section and repointed clauses); design skill (calendar tokens, card anatomy, drag engine, message rules, the now-indicator exception); realtime-and-caching (the capacity read, the month-readiness sentence, the Step 2 note if Q2 replaces FullCalendar); the enforcement backlog row for the literal-colour rule closes when the rule lands; roadmap; this record; a decision-record amendment if Q2 is decided as recommended.

## Durable Homes

Filled at closure: where every lasting fact of this record lives (the spec baseline, the design skill, the freshness contract, the measured scenarios, the message map's location).

## Deletion Pass And Review

To be recorded at closure.

## Completion Evidence

To be recorded at closure.
