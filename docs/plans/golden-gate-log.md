# Golden-Gate Run Log

Append-only record of golden-gate runs, newest first, as required by the roadmap's gate protocol. Record: date, gate, commit/build, environment, fixtures, result, evidence, defects, and anything deliberately skipped.

| Date | Gate | Commit / build | Environment | Fixtures | Result | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-08-04 | `@P1-01` + `GG-00` rerun (post-review) | Fresh production build with the CodeRabbit fixes (committed as the P1-01 review-fix commit) | Local `bun run build` + `bun start` on port 3000 + live Supabase (EU) + R2 `werkflow-documents-prod` | Disposable world `msf72rfo` | **19/19 passed** (2.7m) | Confirmation run after applying the per-slice CodeRabbit review fixes (11 of 20 findings fixed; primary-flag ordering, archived-contact select visibility, error copy/logging, tel-href normalization, fetch-rejection recovery, harness guard/wait). `P1-01` accepted `complete` on this run |
| 2026-08-04 | `@P1-01` (new spec) + `GG-00` rerun | Fresh production build (committed with the P1-01 slice) | Local `bun run build` + `bun start` on port 3000 + live Supabase (EU) + R2 `werkflow-documents-prod` | Disposable world `msf6akzh` | **19/19 passed** (2.6m: 13 `GG-00` + 6 `@P1-01`) | New `tests/golden/p1-01.spec.ts` covers the P1-01 bounded outcome end to end: contacts/sites maintained on the customer detail, a job created with the customer's site + contact through the dialogs (site address auto-fills the Ort snapshot), a second job reusing the same site without a duplicate customer (exactly one visible customer row), site-address edit leaving the recorded job Ort unchanged while the linked site shows current data, the assigned employee seeing site/contact (with click-to-call) on the job page, customer search matching via contact name and site address, and organization isolation. `GG-00` was rerun in the same suite because the job dialog and steps changed. **Finding this cycle (harness design):** `GG-00`'s sign-out test had signed out the employee, revoking the session later specs still needed; sign-out now uses a freshly logged-in invitee session that no later spec depends on |
| 2026-08-04 | `GG-00` (automated v3) | Fresh production build (committed with this extension) | Local `bun run build` + `bun start` on port 3000 + live Supabase (EU) + R2 `werkflow-documents-prod` | Disposable world `msf4ox01` | **13/13 passed** (1.8m) | Extended with the two remaining baseline flows: (1) invite/onboarding — admin invites a seeded confirmed user as Büro through the real dialog (email actually sends via Resend to the bounce-safe test address `delivered+gg-<runId>@resend.dev`), the invitee joins through the real `/auth/callback?invite_code` → login → redemption path, reaches `/dashboard?joined=<orgId>`, sees the Büro-only `/dokumente` library, and appears in the admin's member list; (2) employee inventory take/return on an assigned job (take 3, return 3) with ledger-consistency assertions: `inventory_stock_levels.quantity_on_hand` equals both the sum of `inventory_movements.quantity_delta` and the last movement's `quantity_after` (3 movements incl. seeded `initial_count`). **Findings this cycle:** (1) real defect — `ClockStateProvider` applied a late-streamed server snapshot (`initialState`) over newer client mutations, so a successful clock-in (server row confirmed via trace) visually reverted to "Einstempeln"; fixed by ignoring the snapshot once any local mutation happened (`mutationVersionRef` guard) and refreshing from the server instead. (2) Harness lesson: the invite-join step must be idempotent — a slow first login can outlive its wait; re-opening the invite link while logged in redeems server-side, so `already_member` counts as success. Two intermediate failing runs (one per finding) preceded this pass. No baseline flows remain unautomated |
| 2026-08-04 | `GG-00` (automated v2) | Fresh production build (committed with this extension) | Local `bun run build` + `bun start` on port 3000 + live Supabase (EU) + R2 `werkflow-documents-prod` | Disposable world `msf2sbut` | **11/11 passed** (2.0m) | Extended with: job-linked time tracking (clock in on assigned job, clock out), Realtime freshness (Büro sees admin's new customer without reload, two users signed in simultaneously), mobile-viewport employee job visibility, hardened upload success assertions. **Findings this cycle:** (1) `/kunden` was missing `useRealtimeRouterRefresh` — colleagues' new customers never appeared without reload; fixed in `kunden-content.tsx`. (2) Pre-hydration login submits fell back to native GET putting credentials in the URL; hardened with `method="post"` plus setup retry. (3) Operational lessons: gates must run against a production build on port 3000 (the bucket CORS origin) and never rebuild `.next` under a running server. Remaining uncovered: invites/onboarding flows, inventory take/return |
| 2026-08-04 | `GG-00` (automated v1) | Working tree at harness introduction (committed with the harness) | Local `bun start` production build + live Supabase (EU) + R2 `werkflow-documents-prod` | Disposable world `msf0xf6r`: Golden Test org (admin/Büro/employee) + outsider org | **8/8 passed** (72s) | Covered: role logins, customer creation, job creation with employee assignment, employee sees only assigned jobs, 6 MB direct-to-R2 upload on job page, Büro library visibility, employee blocked from `/dokumente` and `/inventar`, cross-organization isolation, sign-out. **Deliberately not yet automated:** invites/onboarding, time tracking, inventory take/return, Realtime freshness, mobile/responsive paths — extend `GG-00` when those areas next change |
