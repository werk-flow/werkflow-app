# Enforcement-Ladder Backlog

Status: living — last reviewed 2026-09-18

Closed Step 2 conversion: database-readiness recovery is owned by `RealtimeProvider`, not individual pages. The `postgres_changes` system-ready signal closes the gap after channel join at Tier 1. The real-provider route/calendar contract tests the gap, failed and unrelated readiness messages, and obsolete held reads at Tier 2. See [the canonical freshness contract](realtime-and-caching.md#client-freshness-contract).

Route refreshes retain their pending render and one queued follow-up at Tier 1. Two real Suspense contracts cover event bursts and disable, settlement and re-enable at Tier 2. This prevents dropped or interrupting invalidations; the application tests separately enforce the unchanged freshness deadline.

Customer live reads now share the bounded server reader with SSR through an authorized private GET. Confirmed insert overlays settle after an accepted post-save read even when pagination/search excludes the record; pending drafts survive, and rollback resumes cancelled reads. These Tier 1 boundaries have actual authorization/transport and component coverage at Tier 2. See [the customer read contract](realtime-and-caching.md#paginated-customer-live-reads).

The open conversion candidates from the 2026-08-27 three-part audit (code comments, docs, skills) under [decision 0005](../decisions/0005-enforcement-ladder.md). Each row names the rule, where it lives as prose today, and the concrete mechanism that would enforce it. Remove a row when its conversion lands (and annotate the owning doc "enforced by …"); add rows as new lessons appear.

Rows propose implementation directions. Before implementing one, verify its current scope and preserve the owning domain's authorization, historical records, and teardown contracts. A check belongs to Tier 2 even when it prevents an unsafe command from starting. The dated notes below record completed work; the tables contain only open work.

The September 12 organization repair removes persistent membership facts at Tier 1. The actual-reader check `lib/data/membership-freshness.test.ts` catches stale creation, role, revocation and lifecycle results while preserving request deduplication at Tier 2. Disabled live views report no active refresh; a real-hook component check covers abort, re-enable and late-result rejection. These conversions are complete; application acceptance remains in the Step 2 record.

The shared Supabase fetch owner also limits per-process concurrency and reserves foreground capacity at Tier 1. Tier 2 checks cancellation, capacity release, background progress and preservation of each caller's asynchronous context. [Backend request capacity](realtime-and-caching.md#backend-request-capacity) owns the limits and their scope.

The September 12 layout repair separates optional clock/active-job reads from required page authorization. Clock transitions reject unknown or failed state in their shared provider at Tier 1. The actual layout test proves optional reads cannot hold content, and the real clock component contracts exercise delayed, failed and foreign-organization reads at Tier 2. The [readiness reference](realtime-and-caching.md#shared-layout-and-clock-readiness) owns the rule; the Step 2 plan owns application acceptance evidence.

The September 6 [testing restructure](../plans/phase-1/hardening-2026-09/04-testing-system-restructure.md) adds owned group worlds, sessions, and output paths at Tier 1. Tier 2 checks enforce catalog identities and content drift, actual producer selection, group-input qualification and later-failure invalidation, bounded retries, and strict readiness and freshness deadlines. The assertion-to-catalog meaning review remains Tier 3 because matching identifiers and files cannot establish semantic equivalence. The plan records verification status; these mechanisms do not certify the whole application by themselves.

The September 8 planning reconciliation removed the two old catalog-equality/text-hash proposals from the open table. `lib/testing/coverage-map.ts` already owns catalog identity, mapping and wording-drift checks under the current architecture. Semantic clause-to-assertion review remains required; an identifier match does not prove every behaviour.

The September 6 review follow-up removed automatic server startup from every business Playwright configuration, closing the legacy focused-lane development-server fallback at Tier 1. All preflight lanes now require the same recorded production server. `lib/testing/browser-server-contract.test.ts` guards configuration and unconditional preflight ownership at Tier 2. The separate backlog item for ordinary developer commands replacing `.next` remains open.

Stage A of [platform hardening](../plans/phase-1/consolidation-2026-08/platform-hardening.md), on 2026-08-28, added its own protections directly rather than through this backlog: run-policy pins the canary suite to the cloud target and the preflight refuses any run whose `.env.local` routing does not match the requested target (Tier 2); retained-world cleanup refuses a backend mismatch (Tier 2); the untracked-file fingerprint uses NUL-separated git output (Tier 1); local-stack health probes carry named remedies and the canary pins the HIBP copy and migration-history parity (Tier 2). The owner review after Stage A extended the rerun budget to every certification suite — it was golden-only, which let eight same-class audit certification retries run ungated during the Stage A campaign (Tier 2, `scripts/run-playwright.ts`).

Stage B (2026-08-28) retired four Tier 1 rows and two Tier 2 rows by landing them, and added its own protections: the `RealtimeTable` type derives from the one `REALTIME_TABLES` list and the provider generates every binding from it, so a table cannot subscribe without its organization filter (Tier 1 by construction); `useLiveView`/`useRealtimeRouterRefresh` expose no debounce option, so the 150 ms floor cannot be undercut (Tier 1 by construction); `bun run realtime:check` — also part of the local preflight — diffs the list against the database publication and replica-identity state (Tier 2); ESLint bans async `startTransition` callbacks, `setInterval` (named wall-clock exceptions), and `useRealtimeEvent` imports outside the live-view family (Tier 2); replica identity `USING INDEX` limits the old-row data in logical replication (Tier 1). This is not proof of DELETE authorization; the [transport reference](realtime-and-caching.md) records the verification gap found on 2026-09-05.

Stage C (2026-08-29) retired the testing and Realtime-check rows that the hardening pass completed. Audit date ranges now go through `ownedBerlinDateAtOffset()` (Tier 1). Persisted precondition helpers fail fast with the exact recovery grep chain, and the full Wave 1 audit battery follows the current conventions (Tier 2). ESLint rejects raw page-root selectors, positional locators, fixed sleeps, per-test timeout overrides, skipped or focused tests, direct cleanup markers, and `visibleText(...).toHaveCount(0)` in specs. Unit convention tests check serial mode, negative-check presence, staged Golden tags, audit tags, date ownership, and precondition recovery text (Tier 2). The Realtime import ban covers alias and relative imports. The original parity check required INSERT, UPDATE, and DELETE publication flags; the September security follow-up replaces that contract with INSERT/UPDATE only and protected deletion notifications. Stage C left the optimistic-mutation and cross-battery-lock candidates open. The September 5 pass added cooperative lifetime locking; the remaining mutation-outcome inventory is still listed below.

P1-19 follow-up hardening (2026-08-30) retired three more lessons. Contextual document uploads now use one `DocumentUploadTarget` discriminated union from the component boundary through both server actions, so a missing or mixed owner target does not compile (Tier 1). Dispatch-visible fixtures use `dispatchOverviewBerlinDateAtOffset()`, which shares the product's maximum offset and rejects dates outside the panel window (Tier 1); the convention unit test pins the bounds (Tier 2). `bun run types:generate` and `bun run types:check` pin Supabase 2.116.0, Prettier 3.6.2, the DEV project and both generated schemas, so formatter drift no longer hides the schema delta (Tier 2).

The P1-21 CodeRabbit follow-up (2026-09-01) retired the invocation-rules row after two agents mistook PATH failures for a missing installation. The repository now exposes `bun run review` as the only supported path; it targets the configured WSL binary directly and refuses to install or reinstall anything (Tier 1 for agent workflow). PowerShell shims and a WSL PATH link remove both known false-negative probes on this workstation (Tier 1 for host behavior). Unit tests and `docs:check` pin the wrapper and reject raw installer or CLI instructions in agent-facing files (Tier 2).

Owner audit of Stage C (2026-08-29) left two small non-rule cleanups for the next harness touch, so the certified closure sources stayed untouched then: `tests/golden/gg-01.spec.ts` and `tests/golden/p1-03.spec.ts` still carry unguarded chained module state (the precondition module supports `suite: 'golden'` but the retrofit deliberately scoped guards to the audit battery), and `visibleMatchingText` is defined identically in both `tests/audit/support/a1-steps.ts` and `tests/audit/support/p1-13-steps.ts` (fold into one shared support home). The September 5 pass replaced the chained module state with typed persisted checkpoints; the helper consolidation remains separate.

P1-18's product protections landed with the slice, but its run-discipline review exposed seven avoidable fresh-world runs that the original closure did not enforce. The 2026-08-30 follow-up rejects suite-wide “focused” selections and the learned incomplete A1 producer chain before world creation (Tier 2); blocks unclassified or repeated same-class focused retries, requires the mapped P1-16 proof before Golden certification after field-work-pack changes, and checks DEV migration parity in cloud preflight (Tier 2). Only host-side command polling remains Tier 3: the repository can suppress progress output and instruct agents to wait once, but it cannot control the Codex app's command-session polling schedule. That conversion belongs in the agent harness, not this codebase.

The UI/UX hardening pass (2026-09-03 to 2026-09-04, [ledger](../plans/phase-1/hardening-2026-09/02-uiux-hardening.md)) landed its lessons directly. Tier 1: one permissive `uuidSchema` for every validator; the `Field` primitive owning label, marker, helper and error wiring; the `PageShell` column and dynamic-viewport shell; a custom `TimeInput` on every web pointer type; `TableRow`/`ListRow` owning hover through `interactive`; each list rendering header and skeleton from one column definition; `RefreshButton` preserving loaded rows; `PendingSubmitButton` owning the pending interval of plain Server Action forms; `useSettleOnChange` joining props-driven mutations to their authoritative read; `RowActionsMenu` owning a stable menu boundary for rows that can remount under optimistic or Realtime reconciliation; and the shared optimistic-list, pending-row and row-busy primitives. Tailwind candidate discovery is bounded to product-code roots in `app/globals.css`, so retained evidence and repository-local build backups cannot inflate or stall CSS compilation. Tier 2: ESLint bans on the strict uuid check, native date, time, month, week, number, range, checkbox and radio inputs, raw `role="alert"`, hand-rolled page columns, `h-screen`, labels outside a `Field`, the row hover literal, and product-code `useTransition`; the development-time option-count guard in `Select`; the 375 px viewport audit; `lib/ui/skeleton-pairing.test.ts`; `lib/ui/custom-time-input.test.ts`; and `lib/ui/tailwind-source.test.ts`. The Phase 4 proofs showed the option-count guard fires only when a select opens, which left three ten-option enums undetected until a browser run; the September 5 static inventory below closes that conversion. Phase 5 and closure proofs also promoted semantic member navigation to a real link and kept optimistic notification controls mounted through settlement (Tier 1), with persisted-state waits in the shared Golden helpers and P1-23 setup (Tier 2).

Phase 6 exposed a test-architecture debt rather than a reason to discard the test canon. The final campaign attempted the exhaustive audit repeatedly while UI and harness corrections were still moving: 15 failed audit certifications (10 harness assumptions and five product defects) plus one operator-cancelled environment run consumed about 9.1 hours of recorded machine test time across 21 complete-battery attempts. That closure deferred the corrective architecture to the pre-Wave-3 testing-system overhaul: preserve Golden/Audit/Canary responsibilities, flow IDs, business assertions, environment separation and the three-tier enforcement ladder, but make audit consumers independently seeded or checkpointed, expose cumulative run cost and progress, make prerequisite chains explicit, and stop using a full battery as a discovery loop. The incident-level evidence and the bounded closure rule live in the [incident log](test-incident-log.md) and UI/UX [ledger](../plans/phase-1/hardening-2026-09/02-uiux-hardening.md).

The bounded closure audit then retired two product correctness lessons instead of parking them as test prose. `projectLegacyEntriesForWindow` applies approved corrections and makes an out-of-period corrected fact unavailable to the period calculation before interval pairing (Tier 1); `lib/time-accounts/legacy-projection.test.ts` reproduces the August-open/December-close failure and the legitimate move-out-of-window case (Tier 2). This prevents a later-period correction from suppressing an earlier period's `missing_clock` close blocker. Aufträge creation pickers consume the same confirmed client-owned collections as their table and exclude temporary optimistic inserts, so a successful create is immediately available to the next related create without permitting an invalid temporary foreign key (Tier 1); the A1 browser flow is its Tier 2 regression proof.

## Security and infrastructure hardening, 2026-09-06

The [Step 1 hardening pass](../plans/phase-1/hardening-2026-09/05-step-1-security-infrastructure.md) removed dead route handlers and moved trusted-ID lookup helpers into `server-only` modules. Return-path and same-origin helpers enforce callback restrictions at Tier 1. Route inventory, proxy-prefix, header, and SQL grant checks provide Tier 2 prevention. Independent review found gaps in the first action/import checks and older event-ledger coverage; the following repairs supersede those first-implementation claims. The plan owns their test results and deployment state.

The September 7–8 review repairs close the dependency, cloud-grant, and older-ledger candidates:

- Tier 1: the profile-name reader obtains fresh user-scoped operational membership before privileged subject reads. The email-change RPC serializes each account's challenge generation, attempts, resends, reset, and completion claim, with no client table access. Mail delivery has one failure/logging boundary and explicit local capture. Deletion notifications use current-membership RLS; raw DELETE/TRUNCATE publication is disabled. The 11 older event tables receive a guard that rejects direct privileged rewriting and preserves their actual parent cascade/reference-nulling contracts.
- Tier 2: actual action and edge-handler tests cover denied callers and truthful mail failures. SQL tests cover restricted roles, concurrent challenges, replay, immutable history, legitimate cleanup, and deletion notification visibility. Action discovery covers module and inline directives and rejects unsupported exports. The storage check follows the client import graph through relative and dynamic imports and re-exports. Negative fixtures reproduce the earlier escapes. `lib/testing/sql-assertion-files.test.ts` guards multi-file execution and failure propagation.
- Tier 2: `static:dependencies` runs the bounded advisory check on dependency/gate changes and release mode, with exact expiring exceptions. `canary:security` reads live DEV grants and captures ordinary/foreign/revoked Realtime receivers. Publication parity checks require exact deletion-trigger coverage. Local SQL and cloud checks prove their own targets only.
- Tier 1 and Tier 2: the recovery command owns isolated synthetic databases and buckets, refuses external backup input, and stops on failed stages. Focused tests cover safety refusals and failure propagation. Exact backup/restore/permission evidence belongs in the plan and recovery journal.

Authorization meaning, uncertainty after an external Auth write, provider recovery decisions, and legal/retention duties remain Tier 3. Their [control map](security.md) and [runbook](recovery-and-incidents.md) explain the boundary. Helper-name detection cannot prove role correctness; an application cannot infer whether an ambiguous provider request committed or decide the owner's legal obligations.

## Whole-app performance hardening, 2026-09-08

Golden/provider appearance measurements use `locator-observation.ts` to avoid increasing selector retry delays and record browser visibility before later assertion work. Tier 2 real-browser contracts preserve hidden/ambiguous rejection, failed-save and navigation rejection, and every hard deadline. See the [observation incident](test-incident-log.md#2026-09-12-selector-retry-backoff-inflated-an-appearance-measurement). Registered benchmark references were transferred only after a review proved their executed method unchanged; old samples and context entries remain available.

The [Step 2 pass](../plans/phase-1/hardening-2026-09/06-step-2-whole-app-performance.md) landed its calendar and measurement lessons above prose:

- Tier 1: `lib/calendar/range-data.ts` prevents an uncovered or failed window from being reported ready: coverage, generation, and read outcome are typed per dataset and readiness is derived per needed window. `getCalendarFetchRange` owns every view's window; `parseIsoDateRange` bounds absence reads at the action boundary; `createTrailingScheduler` bounds event deferral in the two consumer hooks while the provider delivers events immediately; the pending correction projection accepts only its bounded read port. Query filters and focused port tests enforce pending-only rows.
- Tier 2: reducer tests for out-of-order, partial-failure, scope-reset, and mutation-ownership cases; Berlin DST and month-boundary window tests; query-count tests for the correction projection; scenario registry validation in `checkLatencyEvidence` (unknown id, obsolete version, widened budget, wrong boundary, surplus sample, over budget, regression against a reviewed baseline, unverified required comparison); `getGroupTimingRequirements` refuses a spec whose scenario calls drift from the registry; the coverage of the registry, baseline file, and validator by group fingerprints through ordinary imports.
- Tier 3: the provisional budgets and the typical data profile are declared assumptions until the owner supplies real business sizes; whether a rendered view is usable remains a marker convention plus review; the baseline review reason is a human judgment.

Remaining performance candidates require evidence from a larger workload or a newly measured query bottleneck. Repeated responsibility reads in attention GETs now share one request lifetime; obsolete background GETs carry cancellation to Supabase. Actual read-lifetime and component checks enforce both mechanisms. The planning overflow response already fails explicitly. The attention and calendar GET transports already bypass the browser Server Action queue. The Step 2 plan records the separate deployed script-policy decision.

## UI/UX verification and test reliability, 2026-09-05

The [closed pass](../plans/phase-1/hardening-2026-09/03-uiux-and-test-reliability.md) records these conversions and their qualified baseline and affected-scope proof. Its final local P1-24 and audit checks passed on one candidate, followed by the DEV canary on a recorded cloud build. This is not complete current-candidate audit or integrated Golden certification.

The final personnel follow-up adds explicit accepted-action feedback and native health-evidence form submission through the shared banner and form controls, with values preserved after a rejected upload (Tier 1). Real-component contracts verify accepted receipts, rejected receipts, and upload retry; business checks wait for the loaded correction control and exact persisted receipt identities after a single write (Tier 2). The task-specific evidence qualification is a documented judgment under decision 0006, not an automatic replacement for normal slice or release gates (Tier 3).

- Tier 2: the static Select inventory resolves all 83 current controls and their bounded option declarations. Entity-versus-enum meaning remains a reviewed design decision.
- Tier 2: recorded world cleanup and retained-manifest checks prevent missing archive files from silently permitting cleanup sweeps. A real Playwright discovery test guards the shared lock module against loader incompatibility.
- Tier 2: effective ESLint configuration tests reproduce page-shell escaping errors, static JSX expression bypasses, and exception blocks that silently dropped unrelated restrictions. The fixes compose scope exceptions and test the actual config.
- Tier 1 ownership with Tier 2 browser proof: shared controls own field names and descriptions, searchable-option semantics, keyboard selection, and focus restoration. The isolated component suite tests the real components without creating a business world.
- Tier 2: `build:test` records source/environment digests and a served build ID. Preflight validates the exact workspace and current inputs before accepting a reused production server, including focused lanes. Hosted `build` stays on Next's normal command.
- Tier 2: the runner compares stable discovered and executed test IDs, scopes qualifying proof by target/candidate, checks declared prerequisite graphs, and invalidates evidence if candidate inputs change during execution. Prose-only edits no longer invalidate focused proof.
- Tier 2: a persistent campaign counts complete attempts and elapsed minutes across source/grep/class changes. One-use extensions preserve the classification and exact-stage focused-proof gates. The reporter exposes stage and campaign cost. Superseded 2026-09-14 by the release breaker (`lib/testing/release-breaker.ts`).
- Tier 2: one cooperative lock covers the complete browser/cleanup operation, recorded builds, environment switching, full units, and SQL assertion wrappers. Raw external commands remain outside this mechanism.
- Tier 1 isolation with Tier 2 checks: audit files receive independent worlds. Typed persisted checkpoints carry world identity into retained diagnostics. Golden remains deliberately integrated. A checkpoint is not proof that an arbitrary partial mutation is replay-safe; the remaining replay inventory stays open below.
- Tier 2: refresh settlement distinguishes new props, timeout, and unmount. A missing authoritative refresh produces a visible banner instead of silently ending the pending indicator. Component tests exercise all three outcomes.
- Tier 1 layout ownership with Tier 2 composition and browser geometry checks: contextual document loading and loaded views share their frame, toolbar, and rows. Maintenance headers, records, and loading rows share columns and a content-width breakpoint.
- Tier 1 semantic forms with Tier 2 inventory and application proofs: editable dialogs own native submission, conditional footer associations, and scrolling bodies. Named command/document exceptions remain explicit.
- Tier 1 color roles with Tier 2 computed checks: primary fills and text use separate tokens, and destructive controls share typed states. Token tests and real light/dark button assertions cover text contrast; other surfaces and non-text contrast still require visual review.
- Tier 2 recovery ordering: interrupted runs become retained before archive I/O. A retry preserves the original completion time, cost, classification, and test outcomes while repairing the archive. Disk-backed fault tests cover missing state and failed writes.
- Tier 1 locator ownership with Tier 2 convention checks: positive route test-ID selectors resolve inside semantic `main`; dialog content retains its dialog owner. Shared helpers own artifact, lifecycle, handover, and dispatch sections. The generic check covers local page aliases, fixture bindings, Page-bearing unions, and dynamic IDs across business suites. Privacy-absence checks retain whole-page roots and identity filters without visibility or positional narrowing. External helper return types and later alias reassignment remain review boundaries.
- Tier 1 reconciliation with Tier 2 browser regression: successful lifecycle mutations explicitly refresh their owning route as well as the card snapshot. The component contract withholds Realtime and automatic action RSC updates, and separately checks rejection behavior.
- Tier 2 gesture evidence: month-calendar drags establish source/target hit areas and verify application drag engagement before a single destination release. Warning, cancellation, and persisted-date assertions remain in the business scenarios.
- Tier 1 picker-helper reuse with Tier 2 failure containment: manual-entry member selection uses the canonical listbox/option helper. Separate action and navigation budgets stop missing controls from consuming an entire scenario.

- Tier 2 transport diagnostics: the shared test-client fetch proxy records redacted method/origin and allowlisted nested causes before preserving the original rejection. Seven unit cases cover forwarding, redaction, unchanged HTTP responses, and recorder failures without retries. It covers 12 DB-helper clients and one seed client. This closes the lost-cause observability gap; the underlying Windows connection failure and continuous backend-health evidence remain unresolved.

- Tier 1 personnel-section ownership with Tier 2 component proof: seven confirmed write paths reconcile through the canonical live-view reader independently of route commits. Read progress remains visible without globally disabling unrelated operations. Regression cases hold props unchanged and withhold Realtime, cover read failure, and keep an unrelated dialog available during a held read. The precise earlier framework reconciliation failure remains unresolved.

## Step 3 cleanup and acceptance, 2026-09-13

The [Step 3 record](../plans/phase-1/hardening-2026-09/07-step-3-final-beta-acceptance.md) landed four Tier 2 mechanisms: exported Server Actions without a product importer fail the security unit group (41 dead public endpoints were removed); the proxy-prefix test requires `/:path*` for route areas with nested pages (the `/zeiterfassung` subroutes had escaped the edge redirect); `colorLiteralSelectors` in `eslint.config.mjs` bans hex string literals in JSX product files; and `docs:check` resolves any backticked Markdown name against the docs tree instead of only `docs/`- or `../`-prefixed paths. Tier 1 by single ownership: one file-size, date, duration, initials, role-option, search-normalizer and organization-clients reader each, and every harness RLS read through `withRoleClient`. The release campaign then added three owner-decided harness rules on 2026-09-14 (Step 3 CL-20 to CL-22): harness code under `tests/*/support/`, `lib/testing/` and `scripts/` qualifies groups only through real imports, a recorded pass is reused while every current input is unchanged in content, and a single freshness or readiness sample inside the approved 25%/250 ms tolerance is recorded rather than failed. The review agenda that follows from the campaign lives in the Step 3 closing account. The remaining candidates are in the tables below.

## Pre-Wave-3 step 1: testing-system review, 2026-09-14

The [review](../plans/phase-1/pre-wave-3/01-testing-system-review.md) landed its lessons above prose. Tier 1 by construction: the proof environment identity (`lib/testing/proof-environment.ts`) replaces the WSL address with a token and names backends `local:<project_id>`; code inputs hash their comment-free token stream (`lib/testing/source-content.ts`); each suite's Playwright config, the component runner, the lint rule modules and Bun's test configuration are owned by the kind that executes them, and agent tooling, ignore files and Markdown under `tests/` are not inputs; the measured scenario helpers live in `tests/golden/support/scenario-measurement.ts`, the only support module in the measurement digest. Tier 2: the release breaker (`lib/testing/release-breaker.ts`) refuses a release plan until every failed group has a focused pass on the current inputs and, after two consecutive failed releases, until the incident log names the latest report; `docs:check` check 12 rejects an incident entry without a tier; `test:runs prune` and the 10 GB archive guard (`lib/testing/run-retention.ts`). Decisions recorded in the review: freshness stays a fixed target (D2); the queued Server Action reads, the certification lane, the focus catch-up delay, the reference recalibration and the parallelism cap are the owner's (D3 to D7).

## Pre-Wave-3 step 2: code-quality regression proofing, 2026-09-14

The [record](../plans/phase-1/pre-wave-3/02-code-quality-regression-proofing.md) landed the gates that keep the repository at its Step 3 state without sweeps. Tier 1 by construction: each static group declares its package script in the registry (`lib/testing/test-groups.ts`), `server-only` and `@typescript-eslint/parser` are declared where they are imported, and the timeline width helpers have one home. Tier 2: `static:unused` (knip, `knip.jsonc`) fails on a dead file, export, or dependency; `noUnusedLocals` and `noUnusedParameters` in `tsconfig.json`; `@eslint-community/eslint-comments` with `require-description` beside `reportUnusedDisableDirectives`; `max-lines` at 2,000 for product modules, with the five larger legacy modules capped at their current size; the `ring-offset` selector and the `ui/no-lucide-stroke-width` rule; `lib/conventions/duplicate-helpers.test.ts` (one home per helper, with a frozen list of today's copies that may only shrink) and `lib/conventions/collection-keys.test.ts` (no `key` built from a mapped collection); `docs:check` check 13 (a slice record closed from 2026-09-15 on carries its deletion pass and review). Tier 3 with the reason: the deletion pass and the independent review are judgment; the protocol names the steps and the check requires their record. The certification lane and its campaign budget are gone (decision D4 of pre-Wave-3 step 1). The rows that landed left the Tier 2 table; the rules this step measured and did not adopt are in it now with their counts. On 2026-09-15 the owner decided the rest: both strict compiler flags are on, exported functions under `lib/` carry explicit types (`explicit-module-boundary-types`), type-only exports are part of the knip gate, and status colors are semantic tokens with a lint ban on numbered palette classes (`paletteSelectors`), each Tier 2; short identifiers stay advice for new code and no duplication tool is added, by decision.

## Tier 1 candidates (make it unwritable)

| Rule (prose home)                                                                                                | Mechanism                                                                                                                                                                                                                                                 |
| ---------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Admin client only after `getUser()` validation (architecture.md; `lib/data/cached.ts` comment)                   | Branded `AuthenticatedActor` minted only by `getAuthenticatedUser()`; `createSupabaseAdminClient(actor)` requires it; or one `authorizedAction({role})` factory as the only admin-client export path                                                      |
| Organization scoping is hand-written per query (protocol invariant 2)                                            | `orgScoped(client, orgId)` builder injecting the `organization_id` filter; lint-forbid bare `.from(` outside `lib/data/**`                                                                                                                                |
| `RegisterOpenDialog` placement (open-dialog-context comment)                                                     | Closed 2026-09-17: the primitives render the registration themselves, and the dead `useRegisterOpenDialog` export is deleted the same day                                                                                                                 |
| File bytes through Server Actions ([decision 0001](../decisions/0001-infrastructure-stack.md); r2.ts comment)     | `lib/security/storage-import-boundary.test.ts` already bans `@aws-sdk/client-s3` and `lib/storage/r2.ts` from `app/`, `components/` and `hooks/`. Open scope: whether the server-generated files written by the three `'use server'` modules (`lib/time-accounts`, `lib/work-artifacts`, `lib/work-handover`) need a narrower boundary |
| PENDING time entries filtered as if only `approved` counts (time-tracking comments; two hand filters remain in `lib/time-tracking/actions.ts`, around lines 1312 and 1318) | `getEffectiveTimeEntries()` in `lib/time-tracking/effective-entries.ts` is the single filter and `lib/time-tracking/validation.ts` calls it; route the two remaining hand filters through it. The `status !== 'approved'` filter in `lib/time-accounts/legacy-projection.ts` is the deliberate Tier 1 re-bound, not a candidate |
| Hardcoded German UI strings buried in logic (AGENTS.md)                                                          | A `de` message catalog + lint on string literals in JSX text/aria positions                                                                                                                                                                               |
| Harness mutation helpers can return the optimistic echo (testing.md, section Prepare, submit once, and verify completion) | Step helpers return the persisted row (db.ts read or reload)                                                                                                                                                                                              |

## Step 2 review repairs in verification

The September 12 clock follow-up moves both global background readers to `GET /api/time-tracking-state` so they cannot occupy the mutation queue. The actual route/auth fixture, complete response parser and provider import guard enforce the boundary at Tier 2. The canonical clock reader also checks membership before protected reads at Tier 1; direct and aggregate action denial tests enforce it at Tier 2. The Step 2 plan owns current app/provider verification.

These mechanisms are implemented in the current candidate. The [Step 2 record](../plans/phase-1/hardening-2026-09/06-step-2-whole-app-performance.md) owns acceptance results.

- Calendar reads carry organization/generation ownership; every mutation blocks all refresh entry points. A reconnect signal only reuses a read started after that signal. The real provider/hook contracts hold requests across saves, reconnects, and scope changes. Tier 1 ownership plus Tier 2 behavioral checks.
- Calendar and attention GET handlers share repeated identity and membership wrapper reads only within an explicit request scope. The scope rejects writes; concurrent caller, later-access-change, failure and exact-consumer checks protect its lifetime. Tier 1 scoped API plus Tier 2 actual-route/provider and inventory tests.
- The provider preserves event identities; feature hooks batch once at the shared 150 ms guard, with 1,000 ms maximum deferral. Actual-provider component checks reject a second delay and duplicate burst reads. Independent time reads start together; an actual-action check preserves employee permission scope while rejecting unnecessary manager responsibility reads. Tier 1 ownership plus Tier 2 behavioral checks.
- A changed local WSL origin permits explicit cleanup only after both recorded test organizations and owners match. Original provenance stays unchanged; local endpoint, bucket, identity and replay-denial tests cover the exception. Tier 2 preflight and recovery checks.
- Retained diagnostics restore only their source workload inputs, never acceptance outcomes. Exact-file restoration and stale-file removal are covered by an archive test. Tier 2 restoration checks.
- Calendar window reads use private GET transport outside the browser Server Action queue. Typed JSON validation, real authorization-boundary cases and full optional-field round-trips protect that boundary. Complete calendar and correction readers use bounded pages and identity batches; tests exceed the backend response cap and reject partial failures. Tier 1 shared transport/readers plus Tier 2 boundary checks.
- Jobs, customer, document and inventory pages select bounded identities after global filtering/counting. Service-only RPCs and current server authorization enforce the boundary; `sql:list-pagination` checks data beyond 1,000 rows, tenant/role isolation and whole-set totals. Tier 1 bounded readers plus Tier 2 SQL/browser checks.
- Calendar and work selectors use scoped server search, bounded continuation, and selected-ID hydration. Closed dialogs no longer preload every customer/job/project. Real picker contracts reject stale search/scope responses. Tier 1 readers plus Tier 2 composition checks.
- Measured scenario evidence retains hard deadlines and comparable baseline results separately. Required references fail closed when absent or incompatible. Calibration drafts use qualified successful evidence and require explicit review before activation. Tier 1 qualification plus Tier 2 negative-path tests.
- The mail handlers share authorization and escaping; recovery cleanup attempts all owned planned resources, including ambiguous creation outcomes; fixture mail recipients accumulate; changed-account replay identity is recorded before independent assertions. Focused handler, recovery, and fixture checks cover these repairs.
- Compatible dependency patches removed the existing advisory exceptions. The bounded network gate and narrow exception validator remain. An empty audit is evidence for that registry response, not a permanent claim about dependencies.

## Tier 2 candidates (a check catches it)

| Rule (prose home)                                                                                                                                                                                                                                          | Mechanism                                                                                                                                                                                                                                                                                             |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Every harness read through RLS signs in through `withRoleClient` in `tests/golden/support/db.ts` (decision 0005, Step 3 CL-06; no prose home in testing.md yet) | Open (pre-Wave-3 step 4 tier audit T10, 2026-09-17): a convention test that `signInWithPassword` under `tests/**/support` and `tests/canary/**` appears only inside `withRoleClient`. Before it can land, the four direct callers must be classified: `tests/audit/support/seed-publication.ts`, `tests/audit/support/time-correction-fixtures.ts`, `tests/canary/support/realtime-boundaries.ts`, `scripts/test-server.ts` (either they are not RLS reads and the rule names them as exceptions, or they move behind the helper) |
| Every certification run recorded in the gate log (testing.md)                                                                                                                                                                                              | Superseded 2026-09-14 by the release breaker (`lib/testing/release-breaker.ts`); the certification lane is retired. Historical mechanism: reporter appends the row itself, or `test:runs` asserts manifest↔log parity                                                                                  |
| Evidence verification after a commit or at publication, including docs-only closure changes (testing.md, section Reuse evidence conservatively) | The runner already records a docs-excluded candidate fingerprint and compares it before and after execution. Add an explicit current-tree or publish-time evidence check against the accepted per-target certification records. |
| Complete slices have a Golden tag (protocol)                                                                                                                                                                                                               | Check roadmap `complete` rows against `--list` output                                                                                                                                                                                                                                                 |
| Incident entries written by hand (testing.md, section Diagnose without restarting the whole plan)                                                                                                                                                          | `test:runs classify` appends the incident-log row itself                                                                                                                                                                                                                                              |
| Mutations must invalidate their cache tags (realtime doc)                                                                                                                                                                                                  | Static scan: `'use server'` mutation files must call `updateTag()` via a table→tag registry                                                                                                                                                                                                           |
| Radix dialog imports bypassing the suspension wrapper (freshness rule 6)                                                                                                                                                                                   | Ban `@radix-ui/react-dialog`/`react-alert-dialog` imports outside `components/ui/**`                                                                                                                                                                                                                  |
| Remaining helper-use conventions and canary coverage in convention scans (owner audit 2026-08-29) | Complete helper-use and audit date-ownership checks and include `tests/canary/**`. Golden and audit module/describe state is already checked by `spec-checkpoint-conventions.test.ts`, including describe modifiers and function-expression callbacks. |
| Purple/violet/amber palette bans (werkflow-design) | Landed 2026-09-15 at Tier 2: the `paletteSelectors` lint in `eslint.config.mjs` bans `violet`, `purple` and `amber` numbered palette classes in product JSX |
| Heavy shadows outside floating elements (the ten `shadow-lg`+ sites today are all floating: the clock button, drag previews, the document viewer, a drag tooltip) | The shadow rule needs a floating-element notion the AST does not carry, so it stays a review judgment |
| Auth-config posture and advisor checks (environments.md, supabase skill)                                                                                                                                                               | An auth-config comparison and an advisor check command. Generated-types drift already has `types:check`. Migration-history alignment is enforced separately: cloud preflight and `migrations:check` catch DEV drift before world creation, C9 repeats the assertion in certification, and every local `supabase db reset` validates local history |
| Unguarded local builds can replace `.next` outside the recorded-build workflow (testing.md, section Reuse evidence conservatively) | Extend local ownership checks to remaining unguarded entry points. `build:test` already refuses a port-3000 listener, and certification preflight requires a matching production receipt, workspace process, and served build ID before Playwright starts. Keep the ordinary hosted build unchanged. |
| Destructive migrations unmarked (protocol)                                                                                                                                                                                                                 | SQL lint requiring a `-- @destructive: <reason>` marker                                                                                                                                                                                                                                               |
| Every public table has RLS + org column (protocol invariant 2)                                                                                                                                                                                             | Live dev check with a named global-table allowlist                                                                                                                                                                                                                                                    |
| Roadmap status enum and progress-log linkage (protocol); counter, ready-set, and slice-record checks already exist                                                                                                                                                              | Extend `docs:check` to reject unknown slice states and require the accepted record to link its progress-log entry                                                                                                                                                                                                                                                     |
| Dependency denylist (provider migrations, unapproved AI SDKs, sonner) without a decision record                                                                                                                                                            | Unit test over `package.json`                                                                                                                                                                                                                                                                         |
| Doc counters (version pins, remaining spec references) vs reality; test counts left the docs on 2026-09-03 | `docs:check` extension comparing docs to `--list`/`package.json` |
| Feature-spec review date older than the newest accepted slice naming it as primary spec (capability-map rule "move implemented behavior into the baseline"; audit 2026-09-03) | `docs:check` extension: parse the roadmap primary-spec column and acceptance dates, compare with each spec's `last reviewed` date |
| Closed plans still carrying imperatives an agent might execute (platform-hardening stage prompts, inventory V1 phases; audit 2026-09-03, fixed by past tense and "historical" headings) | `docs:check` extension flagging imperative sentence openings in `closed` docs outside sections whose heading says historical |
| `AlertDialogFooter` containing a raw `Button` (werkflow-design; the one site was fixed in the 2026-09 hardening)                                                                                                                                              | A registry selector                                                                                                                                                                                                                                                                                   |
| Best-effort cleanup or follow-on `catch { console.error }` blocks can be mistaken for silent user-blocking failures (remaining classes after Phase 5 include sign-out cleanup, invite redemption, member-removal follow-up and settings-access fallback)                                                                  | Triage each contract; require a catch either to expose/return the failure or carry a named best-effort annotation proving the primary result is already settled, then add a `CatchClause` selector                                                                                                     |
| jsx-a11y beyond Next's subset; axe sweep (AGENTS.md accessibility)                                                                                                                                                                                         | Enable `jsx-a11y/recommended`; one `@axe-core/playwright` pass per role                                                                                                                                                                                                                               |
| Remaining mutation helpers do not consistently distinguish optimistic UI, committed state, and Realtime convergence (UI/UX Phase 6 campaign); `createJobMaterialLine` and its siblings in `lib/inventory/actions.ts` report `lines_failed` when the hydration after a committed insert fails, and a retry inserts a second line (CodeRabbit, 2026-09-17; owned by the Wave 3 inventory slices that rework the module) | Add typed outcomes or named assertions where the distinction is still missing, then check callers against their required boundary. `expectLiveWithin` already measures from before-submit to observed visibility and records failures; that repair does not enforce authoritative reads in every mutation helper. |
| `scripts/test-server.ts` runs the local preflight as soon as its WSL lease exists, but on a workstation where WSL idles out the lease restarts the VM and every container, and Kong answers 503 for the first seconds; the preflight's three bounded attempts then fail with no run created (pre-Wave-3 step 5, 2026-09-18, first `test:server local` start of the release run) | Bounded gateway probe in `test-server.ts` before the preflight: poll the local REST endpoint until it answers something other than 503 or 000, with the same remedy text on timeout |
| Backend or storage services can fail while the owned WSL lifetime process remains alive (UI/UX Phase 6 campaign) | Add bounded in-run health evidence for the application-to-backend path if needed to distinguish service failure from product failure. Owned address refresh, initial REST/storage/edge probes, and WSL process-loss cancellation are implemented; process liveness does not establish continuous service health. |
| A retained diagnostic can replay a stage after the failed attempt already persisted some of that stage's writes (P1-20 Phase 6 diagnostic)                                                                                                                                                                              | Require each diagnostic stage to declare an idempotent setup/precondition contract; convention-check known mutation helpers and make the runner surface the last completed checkpoint before replay                                                                                                                       |
| Raw local reset and directly invoked focused Bun tests bypass the cooperative lock (testing.md, section Keep groups independent) | Route remaining infrastructure commands through the shared operation lock; full units and SQL package scripts are already covered |
| List readers filter by a list of every id in the organization (`.in('job_id', jobIds)`); the Step 2 pass fixed the manager job list (PF-21) and the other sites are bounded today | A lint selector or unit scan flagging `.in(` whose argument is a mapped id list without a preceding organization filter, with a named allowlist for bounded batches |
| Attention counts are re-derived by eleven readers on debounced events of nineteen tables and database-ready catch-up (Step 2, PF-20) | Measure with the typical profile; a count-only path or a debounced coalesced read per organization, checked by a query-count test like the correction projection's |
| Unpaged organization reads stop at PostgREST's `max_rows` (1,000) without an error, so a `.limit(1001)` overflow check never fires (Step 2, PF-25; calendar and correction readers use complete bounded pages; main lists use server pagination and selectors use bounded remote search) | A unit scan over `lib/` and `app/` that flags `.limit(n)` with `n > 1000` and organization-scoped `select` calls without `.range`, `.limit`, `.single`, or `.maybeSingle`, with a reviewed allowlist for reads bounded by a small parent set |
| Queued Server Action reads (Step 2 PF-23; pre-Wave-3 step 3 D3 added the closed registry `lib/data/background-reads.ts`, Tier 1 by construction, with `lib/data/background-read-http.test.ts` pinning migrated surfaces at Tier 2). Calendar, attention, clock, active-job, the Zeiterfassung page (nine readers), the job page (six) and member status read through GET. Still outside the registry after the fifteen-surface migration of 2026-09-15: the `/mitarbeiter` personnel sections, `getAttentionOverview` on `/aufgaben` (`components/aufgaben/aufgaben-content.tsx`), the service list and detail readers, and `refreshMemberships` in `components/organization/organization-context.tsx` (called by `organization-realtime-bridge` on every catch-up), which reads `organization_members` through the browser client | Register the remaining readers in the same registry and add each migrated file to the import check; use the recorded scenario attribution to pick the next one. |
| An input edit during a running verification is detected only when the running group finishes, so the remaining groups are lost with it (Step 3 run 7; pre-Wave-3 review A7) | A periodic input-drift poll in `scripts/verify.ts` that aborts the running group at once and records the drift as the reason |
| Descriptive names over one- or two-letter identifiers (AGENTS.md; about 1,900 existing sites) | Owner decision 2026-09-15: the rule binds new and changed code only; no rename campaign, no lint rule. Stays prose by decision. |
| Guard clauses over deep nesting (AGENTS.md; `max-depth` 4 finds 33 sites in 12 files) | Flatten the twelve files, then enable `max-depth: 4` |
| A living doc's newest `2026-MM-DD` date may not exceed its `last reviewed` date (docs README maintenance rule; checked by hand today) | `docs:check` extension: scan each living doc for ISO dates and fail when the newest one is later than line 3's review date |
| Route handlers named in docs must exist (`app/api/*` names in security.md and realtime-and-caching.md drift when a handler is renamed or deleted) | `docs:check` extension: compare every backticked `app/api/<name>` in `docs/` with the folders under `app/api/` |
| "testing rule N" references that resolve to nothing (testing.md no longer carries numbered rules; only "testing rule 12" keeps an explanatory paragraph there, and older records cite other numbers) | `docs:check` extension: reject the phrase `testing rule <N>` outside closed records and that one paragraph |
| Backticked `tests/**` paths in skills go stale when a spec or support module moves; `docs:check` resolves docs paths but not these | `docs:check` extension: resolve every backticked `tests/` path in `.claude/skills/**` and `.agents/skills/**` against the tree |
| Token-level duplication (jscpd at 60 tokens and six lines on 2026-09-14: 353 exact clones, 3.24 % of product lines) | Owner decision 2026-09-15: no jscpd gate; `lib/conventions/duplicate-helpers.test.ts` covers the copy class Step 3 found. Revisit only if a new copy class appears. |
| `components/kalender/fullcalendar-view.tsx` still passes `rgb(...)` event colors and an embedded style block (Step 3 CL-D3); converting them to tokens needs a calendar color decision against the design canon, which was deferred to the calendar UX pass | Decide the event colors in [`P1-24a`](../plans/phase-1/slices/p1-24a-plantafel.md), the slice that pass became, and convert the month renderer there; the hex-literal lint cannot see `rgb(...)` strings, so a follow-up selector for `rgb(`/`rgba(` in product JSX closes it |
| `hooks/use-realtime-router-refresh.ts` carries a redundant cancel effect for `enabled === false` that the suspension effect already covers (Step 2 verification, 2026-09-12; left in place because removing it changes a shared hook for no behavior gain) | Tidy: delete the effect on the next touch of the hook, with the route-refresh contract as the proof; not a rule, no check |
| Local date and time formatters (Step 3 CL-D2; 36 module-local `formatDate*`, `formatDateTime*`, `formatTime*` functions in 32 component files, frozen in `lib/conventions/duplicate-helpers.test.ts`) | Each UI pass on one of those surfaces adopts `formatGermanDate`, `formatGermanDateTime` or `formatBerlinDateTime` from `lib/utils.ts` and removes the name from the frozen list when the last copy goes |
| The five capped modules (Step 3 CL-D7): `lib/documents/actions.ts` 4,198 lines, `components/dokumente/document-library-content.tsx` 3,478, `lib/time-tracking/actions.ts` 2,923, `lib/time-accounts/actions.ts` 2,694, `components/inventar/inventory-content.tsx` 2,283 | The `max-lines` cap forces a split before growth. The seams for the two action modules (pre-Wave-3 step 2, 2026-09-14): `lib/documents/actions.ts` splits into `lib/documents/reads.ts` (the 16 readers: `getDocumentLibrary`, the contextual `get*Documents` readers, `getDocumentDetails`, the three signed-URL readers), `lib/documents/folder-actions.ts` (the five folder mutations, `createDocumentFolder` to `deleteDocumentFolder`), the remaining 14 document mutations in `actions.ts`, and the shared authorization helpers (`getAuthorizedDocumentContext` and its siblings) in one `server-only` module; each new file carries its own `'use server'` directive and exports its own functions, because the Server Action test rejects re-exports. `lib/time-tracking/actions.ts` splits the change-request quartet (`getPendingChangeRequests`, `reviewChangeRequest`, `cancelOwnChangeRequest`, `getChangeRequestsForEntries`) out beside `lib/time-corrections/actions.ts` and the job and project context reads (`getTimeEntriesForJob`, `getTimeEntriesForProjectJobs`, `getJobsForPicker`) into a second module; entry mutation and review stay. The split of `lib/documents/actions.ts` also carries the CodeRabbit finding of 2026-09-17: `hydrateDocuments`, `hydrateFolders` and `getFolderBreadcrumbs` throw, and only `getDocumentLibrary` maps that to `documents_failed`; the other sixteen readers surface a generic action error. With the split they return typed failures and every caller maps them |

## Post-Wave-2 documentation audit (2026-09-03)

A fresh-context audit of every doc, skill, and the code they describe found the drift classes [decision 0004](../decisions/0004-documentation-structure.md) predicted: volatile counters restated in living docs, seven incompatible status-line formats, index rows disagreeing with their targets, and product-status prose in `AGENTS.md` that was one wave behind. The prose was fixed in the owning docs the same day. What climbed the ladder:

- Tier 2, landed in `scripts/check-docs.ts`: status line on line 3 in the genre's exact shape; backtick doc paths inside `docs/` rejected outside append-only logs; index status prefixes compared with the target doc; roadmap accepted counter, ready-set recomputation, and complete-row-to-slice-record linkage with date parity; user-flow-catalog section order, unique sequential flow IDs, and invariant counts.
- Tier 2, landed in the harness: `tests/golden/p1-07.spec.ts` and `p1-12.spec.ts` carry their `@P1-NN` tag, so a certification failure in either can satisfy the runner's focused-proof rule without an override.
- Tier 3 by removal: test counts, spec inventories, duration baselines, and "latest parity checkpoint" paragraphs left [testing.md](testing.md), [environments.md](environments.md), and [realtime-and-caching.md](realtime-and-caching.md); the commands that produce them and the append-only gate log are the only homes.

The September 3 audit originally identified proxy prefix parity, the storage server boundary, inventory ledger immutability, documentation dates and dead routes. Completed security conversions are recorded above. Only entries still present in the open tables require work.

Phase 6 retired two stale prose-only bullets from that audit. The 24 entity-list/fixed-enum candidates received their human triage in UI/UX hardening Phase 3. At that checkpoint, static enforcement of the ten-option fixed-enum bound remained open. Feature specs intentionally keep their complete current-product baselines while per-slice mechanics remain in acceptance records, so that structure is not an unresolved owner decision.

The September 5 static Select registry closes the option-count conversion. Entity-versus-enum classification remains a design judgment; the count check does not make that decision.

The September 12 verification follow-up reconciles the A1 browser owner and catalog with SI-006 recorded-time protection. The existing database refusal is Tier 1; allowed/no-history and denied/history SQL plus actual dialog/history assertions are Tier 2. Windows listener discovery retains all recorded-server checks and rejects malformed or conflicting native socket ownership through a validated parser with Tier 2 locale/address-family tests.

Planning overflow is no longer an open implementation conversion: `loadPlanningCalendarEntries` returns unavailable above its 2,000-occurrence or 10,000-assignment bound, and the shared complete-reader tests reject truncation. A larger business workload needs its own representative acceptance evidence before expanding those limits.

Calendar working-time and job visibility now use shared checkboxes with semantic checked state and keyboard activation. The actual filter component contract covers Space, label clicks and independent toggles. Live correction verification explicitly enables working-time visibility before observation. Inventory editing targets the accessible textbox name instead of required-field label decoration. These are Tier 1 component reuse and Tier 2 actual interaction checks. Measured groups retain traces because final comparison occurs after browser teardown.

The September 12 owner-approved performance comparison uses the median of each complete sample set. Tier 1 centralizes comparison in `checkLatencyEvidence`; Tier 2 covers isolated delays, repeatable slowdowns, every hard deadline, missing/duplicate samples, mixed builds/environments and failed operations. Raw observations and historical verdicts remain intact.

Open conversion (2026-09-13): one mutation must not trigger two route renders. The customer create dialog's duplicate `router.refresh()` was removed by hand after the handoff diagnostic counted four list renders per save. No Tier 1 wrapper exists because rule 4 of the freshness contract still requires `router.refresh()` where an action does not call `updateTag`. A Tier 2 check would count `_rsc` route requests per mutation in the real customer browser owner; the component contract renders the list without the dialog, so it cannot see the refresh today.
