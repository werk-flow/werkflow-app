# Testing And Golden-Gate Harness

Status: living — last reviewed 2026-08-30

WerkFlow's regression safety net is a Playwright end-to-end harness that runs the roadmap's golden-gate scenarios (`GG-XX` in [`docs/plans/phase-1/gates.md`](../plans/phase-1/gates.md)) as real multi-role browser journeys against a locally running app. Since 2026-08-28 (decision [0006](../decisions/0006-testing-architecture.md)) the Golden and audit batteries run against the **local Supabase stack** by default; a small **cloud canary suite** proves the live providers against dev Supabase and real R2 (see [environments.md](environments.md) for all three backends). The separate wave-audit battery is the exhaustive catalog-flow layer: golden gates stay lean, while audit coverage must account for every relevant stable flow ID in [`docs/product/user-flow-catalog.md`](../product/user-flow-catalog.md). Wave 1 produced that coverage in wave-end sessions; since Wave 2 every slice ships its own audit spec at acceptance and the wave end only certifies (rule 12; [`wave-2-audit.md`](../plans/wave-2-audit.md)).

## Running

```bash
bun run env:local                           # route .env.local at the local stack (golden/audit)
bun run test:golden:focused --grep @P1-17  # iteration: one focused Golden tag
bun run test:audit:focused --grep @AUDIT-W2-P1-17  # iteration: one focused audit tag
bun run test:golden                         # certification: complete Golden battery (local stack)
bun run test:audit                          # certification: complete audit battery (local stack)
bun run env:dev                             # route .env.local at cloud DEV (canary)
bun run migrations:check                   # compare committed migrations with DEV before a cloud browser run
bun run test:canary                         # certification: the 9-test cloud canary suite
bun run test:golden --target cloud          # wave-end/partner-milestone cloud battery only (D11)
bun run realtime:check                      # publication/replica-identity parity vs lib/realtime/tables.ts (also part of the local preflight)
bun run test:runs list                      # run history with explicit open/cleaned retained-world state
bun run test:diagnostic --grep @P1-17-stage-boundaries --reuse-run <run-key>
bun run test:runs classify <run-key> <product|harness|environment|transient> "<cause>" "<prevention>"
bun run test:runs cleanup <run-key>         # remove one retained disposable world
```

Every run has an explicit **target**: golden and audit default to `local` (the WSL Supabase stack, [environments.md](environments.md)), the canary suite is pinned to `cloud` (dev Supabase + real R2). The preflight refuses a run whose `.env.local` routing does not match the requested target — a forgotten `env:local`/`env:dev` switch fails in seconds instead of sweeping the wrong backend. In local mode it health-checks the stack's REST endpoint, the bundled S3 storage endpoint, and the edge-function runtime (which `supabase db reset` leaves stopped) in three bounded attempts; in cloud mode it checks direct DEV Supabase and R2 endpoint reachability the same way. Retained-world cleanup verifies the same routing: cleaning a world recorded against a different backend is refused until you switch env. Iteration may use the development server. Certification requires a fresh production build and a verified workspace-owned server on `http://localhost:3000`; the preflight rejects a stale server, production routing, retained diagnostic worlds, and an unreachable authoritative backend. `bun run test:runs list` labels each retained world as `open` or `cleaned` and ends with the total number still open. Treat `Open retained worlds: 0` as the clean-inventory result; the historical run result can remain `failed_retained` after cleanup. The runner writes the complete console log, manifest, results, report, trace, screenshot, world identifiers, source fingerprint and build ID under `.agent-logs/playwright-runs/<run-key>/`. It prints only the start, final result and first failure to the interactive terminal. Launch long batteries unattended and react only to the final result or the first archived failure — never poll progress into the conversation (the P1-16 retrospective records progress polling as pure waste). Both configs stop after the first failure because later results in a serial shared world are not independent evidence. Record every gate run in [`docs/plans/golden-gate-log.md`](../plans/golden-gate-log.md). Never run plain `bun test` — unit tests run only via `bun run test:unit`, browser specs through the repository runner, and Playwright discovery via `bunx playwright test --list`. _Enforced (Tier 1): `bunfig.toml` scopes Bun's test root to `lib/`, so a bare `bun test` can no longer pick up browser specs._

### Run lanes and failure recovery

- **Iteration** is focused and requires `--grep`. Before creating a world, the runner discovers the requested and complete suite with Playwright `--list` and refuses a selection with zero tests or every test in the suite. A suite-wide tag such as `@CANARY` is certification, not focused proof.
- **Certification** runs a complete battery on frozen source, a fresh build and a fresh disposable world. It never reuses a failed world.
- **Diagnostic** requires both `--grep` and `--reuse-run <run-key>`. It keeps the failed run's organization, users and database state on the run's target backend (the local stack for battery runs, cloud DEV for canary runs), restores the role sessions, and reruns only the failing stage. This is the supported answer to a late failure: inspect and continue from the actual retained state instead of replaying 100 earlier tests or fabricating a lookalike state.
- Every failed browser run is archived and retained automatically. Classify it before another attempt. Two consecutive focused failures of the same class for the same suite, target and grep block another fresh-world iteration unless the operator supplies a concrete `--override-rerun-budget` reason. Cleanup is explicit after diagnosis. Certification refuses to start while any retained world is still live.
- A passed diagnostic proves the proposed explanation against the retained state. It is not acceptance evidence; the final certification still starts clean.

### The cloud canary suite

`tests/canary/canary.spec.ts` (`@CANARY`, nine tests, target well under 8 minutes) is the only battery that runs against the cloud per slice. It owns exactly the behavior the local stack cannot prove: real login and session refresh across protected navigations, a browser R2 upload with a signed-download byte round trip, cross-session Realtime delivery through cloud infrastructure (asserted against the D4 latency budget via `expectLiveWithin`), the invite flow with a real Resend mail, an organization-isolation smoke, clock-in/out with a persisted canonical session and segment, a server-action write with persisted read-back, the HaveIBeenPwned leaked-password rejection copy (HIBP needs internet — canary-only by nature), and DEV-migration-history parity with the committed files. Cloud preflight now checks that parity before browser setup or world creation; C9 repeats the assertion inside the accepted battery so drift during a run still fails certification. `bun run migrations:check` exposes the same read-only check for diagnosis.

**Growth rule (owner requirement, decision D10):** the canary is open to additions by future agents, but must stay short. Add a test only for behavior that can only be proven in the cloud or is distinctly more valuable proven there; anything provable locally belongs in the Golden or audit battery. Every canary addition or removal is recorded in the [platform-hardening ledger](../plans/platform-hardening.md) with its reason.

Slice acceptance since decision [0006](../decisions/0006-testing-architecture.md): full Golden and audit certification against the **local** stack, plus a green canary run against the cloud. The full battery runs against the cloud only at wave-end certification gates and before owner-named partner milestones (D11).

Hard-earned operational rules (2026-08-04):

1. **Run gates against a production build**: `bun run build`, then `bun start`, then the gate. `next dev` compiles routes on demand mid-test and produces flaky timeouts; the dev server is for iterating on a slice, the production build is for the gate that counts.
2. **Port 3000 only.** Direct-to-R2 uploads are CORS-authorized for `http://localhost:3000`; a server on any other port makes browser uploads fail with misleading symptoms.
3. **Never run `bun run build` while a server is serving `.next`.** The rebuild deletes the running server's chunk files; pages then load without JavaScript (broken hydration, forms fall back to native submits). Stop the server, build, start again.
4. **Retain failures, clean deliberately.** Failed worlds are retained automatically and registered by run key. Use diagnostic reuse, then `bun run test:runs cleanup <run-key>`. `KEEP_WORLD=1` is only an exceptional manual override; certification will not run until retained worlds are resolved.
5. Chromium is already installed (`bunx playwright install chromium` has been run on this machine); do not reinstall unless `playwright test` itself reports a missing browser.
6. `GOLDEN_BASE_URL` can point the run at another server, but direct-to-R2 uploads will fail on any origin the bucket CORS does not allow — stick to port 3000 unless the CORS policy was deliberately extended.
7. **The production server must outlive the entire suite.** Start `bun start` as a detached/background process with no tool time limit and health-check `http://localhost:3000` before starting Playwright. A foreground server under a tool timeout dies mid-suite and invalidates the whole run (`ERR_CONNECTION_REFUSED` on every remaining test — this happened in the P1-05 cycle).
8. **Separate iteration from certification.** Order per slice: implement → self-review and every intended quality/review pass → review fixes → statics and units → fresh production build/server → focused iteration proof → required certification batteries. Do not start a complete battery to discover ordinary defects. The runner's changed-file impact registry requires a current-fingerprint focused proof before certification where a known shared surface has a narrower owning scope; its first mapping makes changes to `components/auftraege/field-work-pack-page.tsx` prove `@P1-16` before a complete Golden run. Extend that registry when an incident establishes another stable mapping. Any later application or Golden-code change invalidates its build and certification evidence; an audit-only fix reopens only the scoped audit evidence allowed by the phase protocol. Post-freeze application fixes batch into one delta-scoped review pass per the CodeRabbit protocol ([coderabbit.md](coderabbit.md), rule 6); only an authorization or data-integrity fix reopens review immediately.
9. **After any fix, prove the affected scope first.** The runner requires `--grep` in iteration, verifies that the grep selects fewer than the complete suite and records a source fingerprint. A full-run retry is blocked until the failed run is classified and a later focused run covering the failed spec passes on the current source. Known serial dependencies are also checked from discovered test titles before a fresh iteration world exists: the learned A1-02/A1-03 selection is rejected unless its A1-01/A1-07 producer chain is included. A retained-world diagnostic may select only the failing stage because its producer state already exists. New slice specs ship stage-split at persisted boundaries (see Conventions) so a late failure can be diagnosed without replaying its setup.
10. **A failed full run is never confirmation, and full reruns have a budget.** The first retry requires classification plus a focused proof covering the failed spec; interrupted and timed-out full runs count as consumed attempts. If two consecutive full attempts fail in the same class, the runner blocks a third unless the operator supplies a concrete `--override-rerun-budget` reason after investigation. Never blind-retry. The durable incident entry records the run key, class, evidence, cause, fix and prevention.
11. **Never trust an existing port-3000 server after a rebuild.** After `bun run build`, stop the verified workspace listener, start the new build in a fresh detached process, and health-check it. Certification preflight compares the listener start time with `.next/BUILD_ID`, checks its command line and refuses an unverified listener.
12. **Audit catalog coverage is exhaustive and many-to-many.** Every user-flow bullet relevant to an audit session has one stable catalog flow ID, and every relevant ID must appear in at least one ledger row. A ledger row or Playwright test may cover multiple catalog IDs, and a catalog ID may span multiple rows/tests; test count never needs to equal flow count. Mapping an ID means the actual assertion bodies evidence every observable clause of that catalog bullet, including roles, defaults, warnings, denials, state transitions, persistence, privacy, and negative promises. A title, helper call, implementation path, or partial assertion is not full coverage. Record partial mappings as `partial`, name the missing clauses, and supplement them before closure. Manual evidence may replace Playwright only when automation genuinely cannot observe the behavior and the owner explicitly approves that named exception. Session closure requires set equality between the catalog IDs owned by the session and the union mapped in its ledger rows, every mapped bullet fully evidenced, and zero partial or unmapped IDs. A material catalog-wording change reopens that ID until its mapped assertion bodies are rechecked. **Since Wave 2, this rule applies per slice, not per wave-end session:** every slice ships its own audit spec in `tests/audit/wave-N/` mapping all of its catalog flow IDs, with ledger rows closed in the wave's audit doc as part of slice acceptance (see `docs/plans/wave-2-audit.md` and `docs/plans/phase-1/protocol.md`). The wave-end audit shrinks to a certification gate: catalog-vs-ledger set equality, one full Golden run plus the full wave battery on one build, and nothing else. Wave 1's retroactive R1 reconciliation is the recorded cost of doing it the other way.
13. **Assert persisted state, never the optimistic echo.** After any mutation, the success assertion must read state the server actually committed: a fresh navigation, a second session/role, the read-only `db.ts` helpers, or a UI element that only renders from refetched data. The instant visual echo of an optimistic update can be green while the server write failed or never committed — a Wave 1 audit test asserted a checklist item the admin's optimistic state showed, the insert had not committed, and the employee's session correctly saw nothing while the test passed. Where an optimistic marker exists (pending style, spinner), wait for it to CLEAR before asserting. This generalizes the older "assert resulting state, not flashes" gotcha into a hard rule.

Playwright writes three artifacts per failure into `tests/golden/.results/<test>/`:

1. **`error-context.md` — read this first.** It contains the failing assertion plus a YAML accessibility snapshot of the page at failure time. Most fixes come straight from seeing what roles/names the page actually exposes (e.g. the employee picker is a `combobox`, its options are `button`s inside a `listbox`, dialogs sometimes close without a success flash).
2. `test-failed-1.png` — screenshot.
3. `trace.zip` — full replay (`bunx playwright show-trace <path>`), rarely needed.

Known interaction gotchas are documented as comments in `tests/golden/support/steps.ts` — read them before writing new steps. The recurring classes so far: duplicate desktop/mobile text nodes (use `visibleText` for a positive assertion and `textInDom` for absence), Escape closing the whole dialog instead of an inner popover (dismiss by clicking elsewhere), success flashes that do or don't exist per dialog (assert resulting state, not flashes), upload dialogs whose "abgeschlossen" counter includes failed files (also assert the absence of error text), and a pre-hydration login race (the setup retries; the form itself is hardened with `method="post"`).

Before rerunning anything, classify the failure: **product defect** (fix the app; for a non-trivial defect, follow the `diagnosing-bugs` skill's feedback-loop discipline), **harness defect** (fix the step/locator, then focused spec first), **environment** (server died, browser missing, sandbox — fix the environment, the run proves nothing about code), or **known transient** (the documented Resend timeout and Realtime-freshness intermittents — rerun before debugging code you didn't change). Two failures of the same class in a row mean stop and investigate instead of trying variations.

The **refresh-interrupted dialog** class deserves its own note because it is a product pattern, not test flakiness: the app's Realtime subscriptions trigger refreshes, and a refresh landing while a dialog is open can remount or reset it mid-interaction (lost typed input, a reopened form snapping back, a "second Uhrzeit locator replaced under the click"). Since the Stage B consolidation every live surface consumes Realtime through the live-view primitive, whose dialog suspension plus one queued catch-up is exactly the fix for this class ([realtime-and-caching.md](realtime-and-caching.md)). A mid-dialog reset therefore points at a surface that bypasses the primitive (lint should already have caught it) — treat it as a candidate product defect, not a locator problem.

A class documented 2026-08-21: **stalled server-action POST**. Signature: a dialog frozen on its pending state („Wird gelöscht...", a disabled „Speichern") for 60s+ while the URL never changes — the browser's server-action request (or its response) hung, even though the underlying database write often committed (verify via the Supabase edge logs: the REST call may show 2xx at the stall time). This is neither a locator problem nor, usually, app logic: during the M2 consolidation session it hit customer deletion, assignment saves, and even harness logins on the same day the dev project migrated compute. Since then the server-side Supabase clients carry a 30 s fetch timeout (`lib/supabase/fetch-with-timeout.ts`), so a stalled request rejects and surfaces through normal error handling instead of hanging forever; post-delete redirects additionally hard-navigate. If this signature reappears, treat it as environment first (dev-project health, network), and check the edge logs before touching app code.

A further environment class documented 2026-08-09 (numbers from that era's 90 s budget and smaller suite; the class itself survives any budget): **long-run latency**. Under heavy machine or file-sync load, per-test times balloon suite-wide and dialog interactions of _old, long-green_ tests can exhaust even today's 180 s budget (element mounted but "not visible" for over a minute while paint stalls). Signature: different tests fail across attempts, per-test times balloon suite-wide, and the focused spec passes at normal speed. Classify with a focused run; do not "fix" green tests — rerun in a quieter environment.

A test that passes must mean the business outcome happened: pair every positive assertion with the state it produced (row exists, URL changed, count is zero for the other role) rather than trusting transient UI feedback.

## Architecture

| Piece                                  | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `playwright.config.ts`                 | Serial execution (shared world state), German locale, Europe/Berlin, screenshots/traces on failure, and a target-keyed 180 s local or 300 s cloud scenario budget                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `playwright.audit.config.ts`           | Separate serial wave-audit battery with the same target-keyed budget; shares the golden world artifacts and therefore never runs concurrently with the golden suite                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `playwright.canary.config.ts`          | Nine-test cloud-provider battery with a 300 s scenario budget                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `scripts/run-playwright.ts`            | Discovers exact selections before setup; enforces lane size, learned serial prerequisites, changed-file focused-proof requirements, source fingerprints, quiet logging and focused/full rerun budgets                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `scripts/playwright-preflight.ts`      | Rejects production routing, target mismatches, unreachable backends, DEV migration-history drift before cloud world creation, stale production servers and certification runs with retained worlds; runs `realtime:check` for local targets                                                                                                                                                                                                                                                                                                                                                                                                              |
| `scripts/manage-playwright-runs.ts`    | Lists, classifies and cleans archived runs and retained disposable worlds                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `tests/golden/global-setup.ts`         | Removes only unretained leftovers, creates or restores a world, refreshes and protected-route-checks all four role sessions, and generates a 6 MB upload fixture                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `tests/golden/support/run-state.ts`    | Owns run manifests, failure archives, retained-world state and cleanup bookkeeping                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `tests/golden/support/run-reporter.ts` | Captures counts/failures and archives Playwright artifacts while keeping interactive output quiet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `tests/golden/support/sessions.ts`     | Creates, validates and refreshes role-bound browser storage for the expected organization                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `tests/golden/support/seed.ts`         | Creates/destroys the disposable world with the service-role key: primary org (admin/Büro/employee), outsider org (isolation checks), active subscriptions, deterministic profile names. Teardown deletes R2 objects under both org prefixes, org rows (everything cascades), subscriptions, and auth users                                                                                                                                                                                                                                                                                                                                               |
| `tests/golden/support/world.ts`        | The world contract and artifact paths                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `tests/golden/support/fixtures.ts`     | `adminPage` / `bueroPage` / `employeePage` / `outsiderPage` fixtures bound to the saved sessions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `tests/golden/support/steps.ts`        | **The reuse mechanism**: named business steps (`createCustomer`, `createJob`, `uploadDocumentOnJobPage`, …). Specs compose steps; when a slice changes UI, update the step once and every gate follows                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `tests/golden/support/live.ts`         | `expectLiveWithin`: the D4 latency-contract assertion (2 s target; 15 s hard budget, local and cloud) used by the key cross-session checks (GG-00, P1-10, P1-12, canary C3); appends every measured latency to the run archive (`live-latencies.ndjson`)                                                                                                                                                                                                                                                                                                                                                                                                 |
| `tests/golden/gg-00.spec.ts`           | The baseline gate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `tests/golden/gg-01.spec.ts`           | The `GG-01` gate: client-request capture, exactly-once conversion, closure, and request authorization boundaries                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `tests/golden/p1-01.spec.ts`           | P1-01 contacts/sites slice proof (`@P1-01`): snapshot stability, assigned-worker context, search, isolation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `tests/golden/p1-03.spec.ts`           | P1-03 personnel-identity slice proof (`@P1-03`): versioned conditions, account linking, offboarding history, isolation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `tests/golden/p1-04.spec.ts`           | P1-04 schedules/holidays slice proof (`@P1-04`): role-aware targets, effective dating, closure days, missing-plan exceptions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `tests/golden/p1-11.spec.ts`           | P1-11 planning slice proof (`@P1-11`): recurring series, edit scopes, occurrence-scoped teams, conflicts, legacy compatibility                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `tests/golden/p1-05.spec.ts`           | P1-05 scoped-responsibility, substitute, four-eyes, owner-safety, RLS, and organization-isolation slice proof (`@P1-05`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `tests/golden/p1-06.spec.ts`           | P1-06 vacation request/decision/balance/target/calendar/clock-block slice proof (`@P1-06`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `tests/golden/p1-07.spec.ts`           | The `GG-02` gate (`@GG-02`, P1-07 exit): schedules/target hours, overlapping leave, delegation-following attention, approve/reject/withdraw, notification deduplication, pattern audit, employee transparency, RLS/isolation                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `tests/golden/p1-08.spec.ts`           | P1-08 sickness slice proof (`@P1-08`, sorts last, dual-mode): self-report/office entry, retroactive/open-ended/half-day cases, target effects, neutral calendar, two-audience notifications with version re-surfacing, evidence state, clock-in notice, privacy-matrix RLS, isolation                                                                                                                                                                                                                                                                                                                                                                    |
| `tests/golden/p1-09.spec.ts`           | P1-09 teams/qualifications slice proof (`@P1-09`, sorts last, dual-mode): date-effective teams without authority, curated skills/certifications, assignment coverage and reasoned overrides, apprentice signal, expiry/renewal history, self/manager/colleague/outsider privacy matrix                                                                                                                                                                                                                                                                                                                                                                   |
| `tests/golden/p1-10.spec.ts`           | P1-10 customer-relationship slice proof (`@P1-10`, sorts last, dual-mode): source-linked filtered timeline, owned follow-up lifecycle and attention, communication guidance/exception audit, cross-manager Realtime, direct-URL denial, and manager/employee/outsider RLS matrix                                                                                                                                                                                                                                                                                                                                                                         |
| `tests/golden/p1-12.spec.ts`           | The `GG-03` gate (`@GG-03`, P1-12 exit, sorts last, dual-mode): parked backlog context and the labeled legacy missing-context state, honest unscheduled dispatch with readiness (tools „nicht bewertet"), traceable unscheduled→scheduled target transition that preserves the independent parking blocker, revision-bound acknowledgement invalidated by a material move, challenge/keep-resolution/re-confirmation, customer commitment surviving an atomic batch reschedule as a visible mismatch and superseding re-commit, dispatch/rescheduling event history, cross-manager Realtime, employee/outsider RLS matrix, and zero actual-time creation |
| `tests/golden/p1-13.spec.ts`           | P1-13 slice proof (`@P1-13`, sorts last, dual-mode): immutable published job template, materialization into the existing checklist primitive, no stock/schedule side effects, future-only version changes, direct project planning, and employee/outsider template-management denial                                                                                                                                                                                                                                                                                                                                                                     |
| `tests/golden/p1-14.spec.ts`           | P1-14 slice proof (`@P1-14`, sorts last, dual-mode): atomic time-start execution, employee-owned blocker resolution, parking/execution separation, reasoned project override/derivation and lifecycle RLS isolation                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `tests/golden/p1-15.spec.ts`           | P1-15 slice proof (`@P1-15`, sorts last, dual-mode): assigned-field capture, manager review, typed Aufmaß, lifecycle evidence/customer/signature gates, organization isolation and teardown                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `tests/golden/p1-16.spec.ts`           | P1-16 slice proof (`@P1-16`, staged): setup/first viewport, authoritative field execution, and terminal/role boundaries split at persisted diagnostic stages                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `tests/golden/p1-17.spec.ts`           | The `GG-04` gate (`@P1-17`, `@GG-04`, sorts last, staged): persisted template/dispatch/target setup, assigned field execution and exact evidence, office review/preview/atomic package release, attributed withdrawal/correction/successor release, and responsibility/organization boundaries                                                                                                                                                                                                                                                                                                                                                           |
| `tests/golden/p1-18.spec.ts`           | P1-18 slice proof (`@P1-18`, sorts last, staged): honest site-owned registration, exact work/artifact/handover/document history, assigned-job field projection, cross-manager Realtime, replacement/correction retention and employee/outsider boundaries                                                                                                                                                                                                                                                                                                                                                                                                |
| `tests/golden/p1-19.spec.ts`           | The `GG-05` gate (`@P1-19`, `@GG-05`, sorts last, staged): existing-owner setup, direct and request intake, exact triage/relation/job links, existing dispatch and field projection, exact evidence/follow-up ownership, cross-manager Realtime and employee/outsider boundaries                                                                                                                                                                                                                                                                                                         |

## Conventions

- One spec file per golden gate, tagged `@GG-XX`, tests in `serial` mode.
- **Every new Golden slice spec ships stage-split — one monolithic slice test is a review flag.** Follow the P1-16 model: separate serial tests at persisted stage boundaries with stable per-stage grep tags (`@P1-XX-stage-setup`, `-execution`, `-boundaries`, or slice-appropriate names). Each later stage verifies its persisted precondition through a read-only database helper or a reloaded UI fact, never module-local memory, so a diagnostic run can resume exactly the failed stage on a retained world without replaying its predecessors. Stage boundaries also keep cheap checks (role denial, privacy) from hiding behind expensive ones (uploads, material movements).
- Wave-audit specs follow `docs/plans/wave-1-audit.md`. Each session ledger records stable catalog flow IDs and concise whole-bullet clause evidence per rule 12; do not infer completeness from test counts or titles.
- New slices extend `steps.ts` with their business actions and add/extend the gate spec named by their slice index row in `docs/plans/phase-1/roadmap.md`. Do not put raw selectors in specs.
- Use `visibleText()` only for positive text assertions. Pages often render the same text twice in a desktop table and a hidden mobile card, so the helper selects a visible instance.
- Use `textInDom()` for zero-count text assertions. Privacy and role-boundary checks must inspect every matching DOM node, including hidden responsive renders. ESLint rejects `visibleText(...).toHaveCount(0)`.
- Scope spec locators by a semantic row, section, dialog, or role. ESLint rejects raw CSS selectors rooted at a Playwright `Page`, including aliases and pages returned by popup helpers. It also rejects `.first()`, `.last()`, `.nth()`, fixed sleeps, per-test timeout overrides, skipped or focused tests, and hard-coded cleanup markers in spec files. Put a bounded exception in a named support helper and document why the semantic API cannot express it.
- Absence assertions (`toHaveCount(0)`) prove role/organization boundaries; every gate should include at least one negative check. The unit convention test checks only the structural presence of a negative form. Review still verifies that the assertion proves the intended boundary or absence of side effects.
- Success signals differ per dialog: some show a flash (`Kunde erfolgreich erstellt!`), some close immediately — assert the resulting row/state, not only flashes.
- Seeded identities use `@werkflow-golden.test` emails and org names prefixed `Golden Test SHK` / `Fremde Firma`; the invite scenario additionally uses Resend's bounce-safe test address pattern `delivered+gg-<runId>@resend.dev` so the real invite email sends without harming sender reputation. The leftover cleaner keys on exactly these markers. Never use these markers for real data.
- `tests/golden/support/db.ts` holds read-only service-role lookups for assertions the UI cannot prove (the invite code inside the email link, stock-ledger consistency). Specs still drive every user-visible action through the UI.
- **Specs inherit state from every earlier spec** (serial, alphabetical, one shared world): members joined in `@P1-03`, schedules and the holiday calendar from `@P1-04`, and so on. A step that passes in a focused run can legitimately fail only in the full run because names appear in more places or extra action buttons exist — that is the full run doing its job. Scope locators semantically (the exact section/row, `exact: true` names), never by count or position. State `@P1-06` leaves behind: `leave_approval` ends in `selected` mode with the admin as sole holder (Büro and the invited Büro member lost it), the employee has an extra Vollzeit condition valid from the run day + 1 carrying 30 vacation days, the employee keeps one approved future half-day vacation and one rejected request, and Büro keeps one approved future single-day vacation.
- **`GG-02` placement and dual-mode design (P1-07):** the gate spec lives in `p1-07.spec.ts` so it sorts AFTER every slice spec — it inherits the full suite-end state instead of injecting business facts that six earlier specs would silently inherit (the rejected alternative was seeding dedicated fixtures and running the gate early). Because the validation ladder also runs it focused on a fresh world, the spec is deliberately dual-mode: its first test pins the responsibility state itself (`time_approval` → employee sole, `leave_approval` → admin sole — a recorded no-op re-selection in the full run), it creates and closes its own client request instead of relying on GG-01's leftover, and every count expectation (inherited notifications, unified badge numbers) is derived from the database at runtime rather than hardcoded per mode. State `@GG-02` leaves behind (nothing runs after it, but seeder/`KEEP_WORLD` debugging should know): two extra no-op responsibility configurations, one closed GG02 client request, new schedule versions valid from run-day+1 for the employee (Mo–Fr 6h) and Büro (Mo–Fr 8h), one approved Büro manual time entry from yesterday 10–11, and for the employee one extra withdrawn, one cancelled, and one rejected vacation request plus attention read markers/events.
- **Date expectations come from the app's own stored state and in-code rules**: `@P1-06` reads the person's schedules/conditions/holiday calendar through `getTargetContextForRecord` (`tests/golden/support/db.ts`) and computes consumed days and weekly Soll with the same `lib/vacation/balance` and `lib/personnel/targets` functions the product uses — hand-rolled date logic in specs drifts in holiday weeks.
- **Expect Realtime-driven re-renders mid-step.** Re-locate a row after any mutation that re-renders it (a saved locator can hold a detached node), and never re-open or re-select a control whose desired value is already set — a router refresh can detach a Radix option between open and click.
- **Fixtures under uniqueness constraints must use dates their spec owns.** Business facts such as `employment_conditions` and `work_schedules` versions, or closure days, collide deterministically when two specs pick the same date. Claim audit offsets through `ownedBerlinDateAtOffset()` in `tests/golden/support/date-ownership.ts`; it throws when a spec uses another spec's range. Use `dispatchOverviewBerlinDateAtOffset()` for a visit that must appear in the manager dispatch panel. It rejects offsets outside the product's shared 0 through 14 day window. Golden offsets remain documented below because their earlier specs predate that registry.
- **Stale-UI action proofs need a frozen page.** The app deliberately self-heals stale views (Realtime events plus a synthetic all-table refresh on `visibilitychange`), so a "click the stale card" test races the app's own freshness machinery and loses intermittently. `@P1-06` freezes the page first — `page.routeWebSocket` swallows the Realtime socket and an init script suppresses `visibilitychange` — which is also the honest simulation of the woken-up-laptop scenario the action-time enforcement exists for. This investigation surfaced a real defect: action-time authorization compared the app clock against database-stamped configuration timestamps, so a machine with a trailing clock briefly kept honoring a just-replaced configuration (fixed with a skew guard in `lib/responsibilities/server.ts`).

- **State `@P1-08` leaves behind** (P1-09 now inherits it): the employee carries one CANCELLED sickness report (yesterday–run-day, `krankheit`, evidence received; events `reported`→`ended`→`evidence_updated`×2→`cancelled` — it no longer affects any target), Büro carries one ACTIVE half-day `kind_krank` report for run-day−1 with evidence required/`pending` recorded by the admin (it halves Büro's yesterday target), and sickness read markers/`attention_events` exist for Büro (two versions of the employee report) and the employee (the cancellation). The spec pins NO responsibility state (sickness authority is the manager role); its mode-dependent expectations (vacation-overlap hint, weekly Soll, notification rows) are all derived from the database at runtime.
- **State `@P1-09` leaves behind** (P1-10 now inherits it): one active two-person team, one expired certification on the employee plus its renewed successor, one skill on Büro, two jobs with qualification assessments and reasoned overrides, an admin-enabled apprentice signal, a future apprentice condition/job at run-day+90, and qualification expiry read state/audit.
- **State `@P1-10` leaves behind** (it sorts last): one run-scoped customer with two contacts, a site, a linked job, communication defaults and contact overrides, audited guidance exceptions, one completed and two open follow-ups, follow-up lifecycle events, and attention read state as produced by the UI journey. The spec is dual-mode, uses no predecessor-owned effective-date keys, and derives identities from the run id.
- **P1-12 date ownership and state left behind** (`@GG-03` sorts last): the spec owns run-day+3 through run-day+10 at 06:00 Berlin (deliberately early so collisions with P1-11's 09:00+ next-month window, when the months overlap, only ever produce the reasoned-override warning that every planning step already handles), run-day+4 as the parked next-review date, and a run-keyed terminal Realtime job visit at run-day+8 08:00. It leaves behind four run-scoped jobs: the PARK job (scheduled run+3 06:00 after its unscheduled dispatch was traceably retargeted; parking context cleared with an `unparked` event), the MAIN job (one visit finally at run+7 06:00 with a 3-revision dispatch — issued → schedule_changed → schedule_changed —, one acknowledged/challenged/kept/re-acknowledged history, one superseded and one active customer commitment), the BATCH job (a two-occurrence daily series shifted to run+9/+10 as exceptions, its earliest occurrence dispatched with a pending confirmation), and the terminal LIVE job whose assigned visit and dispatch make the last test independently replayable. Batch/assessment/dispatch/parking/commitment event ledgers and attention read state exist as the UI journey produced them; no vacation/sickness/schedule/condition rows and no actual time entries were created. Future specs must address these records by run-scoped identities and must not assume an empty dispatch domain.
- **P1-13 Golden state left behind** (`@P1-13` sorts last): two run-scoped published templates (`Wartung <runId>` at versions 1 and 2, `Sanierung <runId>` at version 1), two jobs materialized from the two Wartung versions, and one project materialized directly from Sanierung. The first job has one employee-completed instruction; application/event/provenance rows remain. No inventory movement, reservation, planning occurrence, dispatch, actual time or document is created. Future last-sorting specs must use run-scoped identities and cannot assume an empty template domain.
- **P1-14 Golden state left behind** (`@P1-14` sorts last): four run-scoped jobs and one project, a closed job-linked time session with automatic execution event, one resolved employee blocker, one open manager parking blocker, and a reasoned project override followed by an override-clear event. No inventory movement, reservation, document, signature, message or customer package is created. Future specs must use run-scoped identities and cannot assume lifecycle tables are empty.
- **P1-15 Golden state left behind** (`@P1-15` sorts last): one run-scoped assigned job with a submitted and approved work report, one typed measurement artifact, exact immutable revisions/actions and lifecycle evidence/customer/signature gates. No planning or dispatch mutation, active time, inventory movement, commercial row, message or customer package is created. Future last-sorting specs must use run-scoped identities and cannot assume the artifact or evidence-fulfilment domains are empty.
- **P1-16 Golden state left behind** (`@P1-16` sorts last): one run-scoped assigned job and one denied unassigned job, completed instruction and execution state, one employee draft artifact, one contextual document link, one closed own time entry and paired take/return inventory movements. Stock returns to baseline and no active clock, copied work pack, reservation, consumption, commercial row, message or customer package remains. Future last-sorting specs must use run-scoped identities and cannot assume these job execution domains are empty.
- **P1-17 Golden state left behind** (`@P1-17` sorts last): one run-scoped project child with template instructions, dispatch/acknowledgement, completed field work, closed own time, paired take/return movements, approved work report, measurement, resolved defect, signed customer outcome and exact document evidence; one immutable handover release is withdrawn, corrected and followed by a second immutable release with predecessor linkage. Package documents and append-only handover/lifecycle history remain. No active clock, stock delta, reservation, consumption, invoice, payment, message, public link, service-equipment row, offline queue or external-provider record remains. Future last-sorting specs must use run-scoped identities and cannot assume handover/package-document domains are empty.
- **P1-18 Golden state left behind** (`@P1-18` sorts last): one run-scoped customer site with a replaced root, one active successor and one bounded component; exact identifiers, installation/commissioning origins, job/artifact/handover references, existing-document links and immutable lifecycle/correction events remain. No active clock, stock delta, service request, dispatch, recurrence, contract, commercial row, message, public link, offline queue or external-provider state exists. Future last-sorting specs must use run-scoped identities and cannot assume equipment, identifier, work-link, document-link or event domains are empty.
- **P1-19 Golden state left behind** (`@P1-19` sorts last): one run-scoped customer site, one installed asset, one assigned job, one direct service case, one request-owned service case, one continuation relation, exact equipment/job links, one acknowledged dispatch, one linked work-report revision, one open follow-up and immutable service history remain. No active clock, stock delta, maintenance plan, recurrence, contract, commercial row, message, public link, offline queue or external-provider state exists. Future last-sorting specs must use run-scoped identities and cannot assume service-case, relation, evidence or follow-up domains are empty.
- **P1-20 Golden state left behind** (`@P1-20` sorts last): one run-scoped customer site, installed asset, published work template and reactive service case; one active operational coverage with an open follow-up; one active versioned maintenance plan with generated due work; and one completed due item linked to its normal assigned visit job, planning occurrence, submitted work-report revision, next due date and immutable maintenance history remain. No active clock, stock delta, commercial row, message, public link, offline queue or external-provider state exists. Future last-sorting specs must use run-scoped identities and cannot assume maintenance, coverage, follow-up, visit, planning or evidence domains are empty.
- **P1-21 Golden state left behind** (`@P1-21` sorts last): one run-scoped assigned job with a started lifecycle, one closed stable canonical session containing work, travel, break, standby, call-out and internal-activity segments, one closed legacy-bridged standby session, immutable operation/event history and the original legacy compatibility rows remain. No active clock, open segment, unrecovered sequence, schedule/dispatch mutation, stock delta, commercial row, message, public link, offline queue or provider state exists. Future last-sorting specs must use run-scoped identities and cannot assume canonical time tables are empty.
- **Weekday-dependent expectations must be computed, not assumed.** The first Saturday full run (2026-08-08) exposed a latent weekend-blind assertion in `@P1-04`: after removing a Betriebsruhe day the spec expected „Tagesziel:", but on a schedule-free weekend day the dashboard honestly shows „Laut Arbeitszeitmodell heute kein Arbeitstag." The assertion now resolves today's target through `getTargetContextForRecord` + `resolveDailyTarget` and expects the matching text. Any assertion about "the day's target returns" must go through the resolver.
- **Mid-suite specs are not dual-mode.** Earlier specs may depend on predecessor state. Each dependent Wave 1 or Wave 2 audit test starts with a cheap persisted-state guard through `requireVisiblePrecondition` from `tests/golden/support/preconditions.ts`; if its setup is missing, the error names the exact focused grep chain that restores it. Run that chain in one world. A solo later stage still proves nothing when its guard says that an earlier stage owns the setup.
- **Later specs must not reuse effective-date keys owned by earlier specs.** The first P1-09 full run exposed this with `employment_conditions`: P1-04 already owned run-day and run-day+1 versions, so adding another run-day condition was correctly rejected. A later slice that needs a new effective state must choose a non-colliding date and assess the dependent business action on that same date; P1-09 uses run-day+90 for its apprentice condition and planned job.

- **Manual time entries in specs must lie in the past.** The dialog rejects future times („Manuelle Einträge können nicht in der Zukunft liegen."), so a spec that runs in the morning fails with today's 09:00–10:00; use yesterday with times clear of other specs' yesterday entries (`@P1-05` uses 06–07 and 08–09, `@GG-02` uses 10–11).

## Test Backends (local stack since 2026-08-28, dev/prod split since 2026-08-18)

The Golden and audit batteries run against the **local Supabase stack** — the CLI composition in WSL Docker, schema built by `supabase db reset` from the committed `supabase/migrations/` history, file bytes on the bundled S3 endpoint (bucket `werkflow-documents-local`). The **canary suite** and wave-end cloud certifications run against the dedicated dev project `mbkkzuqjbdvzelqvuzcn` paired with the `werkflow-documents-dev` R2 bucket (EU jurisdiction, localhost-only CORS). Production (`jbgaqpdjauzoocplgdsn`) never sees test traffic. The full environment model, local-stack setup, tooling access, and per-machine onboarding live in [environments.md](environments.md); the decision records are [0003](../decisions/0003-dev-prod-environment-split.md) and [0006](../decisions/0006-testing-architecture.md).

Operational facts for test runs:

- `bun run env:local` / `env:dev` / `env:prod` swap `.env.local` between the gitignored backups; the preflight enforces that the routing matches the run's target. Vercel holds production's env independently, so no local file change can affect the deployed app. Rerun `env:local` after a WSL restart (the stack's address changes) and rebuild before certification.
- Local parity is explicit, not assumed: `supabase/config.toml` mirrors the cloud auth posture (password length, OTP, confirmations), auth mail lands in the Mailpit capture, the invite edge function logs instead of sending without a local Resend key, and the HIBP leaked-password check exists only in the cloud — the canary owns it. Any new parity gap goes to the [platform-hardening plan](../plans/platform-hardening.md)'s open decisions before it is coded around.
- Current duration baselines: **132/132 in 24m29.720s** for the local Golden battery (run `2026-09-01T004616166Z-8dd36a`, world `mthy5cf9`, build `n8q4IoBAKq-Pb1_jwGEso`), **117/117 in 51m10.909s** for the complete local audit battery (run `2026-08-31T235433509Z-2df998`, world `mthwauhj`, same build), and **9/9 in about 2m08s** for the latest cloud canary (run `2026-09-01T021822466Z-bf7ad3`, world `mti1fsvi`, build `W8NUPOa9DKz8wQ221VTpM`). The last cloud full-battery baseline was 110/110 in 56.6m on Micro compute (2026-08-28, world `mtc767kd`), which remains the reference for wave-end cloud runs. Read durations against the current suite size before suspecting a regression. Classify per rule 10 instead of looping full reruns.
- The teardown leftover sweep executes against whatever backend `.env.local` names — with target enforcement that is the run's own backend. Retained-world cleanup refuses a backend mismatch.

## Unit Tests

Pure-logic unit tests run with `bun run test:unit` (Bun's test runner over `lib/`). The first real candidates landed with `P1-04`: `lib/personnel/holidays.test.ts` asserts the official German holiday lists for the current and next year (a legal change fails CI so the in-code dataset is updated deliberately), and `lib/personnel/targets.test.ts` covers the target-resolution contract across part-time, date-effective schedule changes, holiday/closure zeroing, region history, and the labeled fallback cascade — including the historical cases the UI specs cannot pin to fixed dates. `P1-05` adds `lib/responsibilities/resolution.test.ts`: fixed-role fallback, direct configuration precedence, delegation start/end boundaries, expiry, early end, inherited role scope, deterministic defensive precedence for anomalous overlaps, and self-approval are pure date/authorization contracts and stay out of browser timing assertions. `P1-06` adds `lib/vacation/balance.test.ts`: entitlement-per-year resolution (mid-year condition changes, missing values), vacation-day counting across weekends/holidays/closure days/schedule-free days/the labeled default source/half days/year-crossing ranges, the absence-extended daily-target contract, and balance arithmetic including snapshot stability under retroactive schedule changes. `P1-07` adds `lib/attention/resolution.test.ts`: item-identity keys, per-viewer deduplication across authorization paths, decision-notification facts (approve → cancel changes the version of the SAME item), read/unread versioning, the notification window boundary, and open-since day arithmetic — the deduplication and due-state rules the GG-02 UI assertions rest on. `P1-08` adds sickness cases across three files: sickness-span target zeroing (full/half, holiday-first ordering, vacation-wins attribution on double-covered days, span boundaries) in `targets.test.ts`, sickness-report notification facts (version from availability-relevant fields only — corrections re-surface, evidence bookkeeping does not; cancellation timing; two-audience dedup) in `resolution.test.ts`, and the honest open-ended range formatting in `lib/sickness/types.test.ts`. `P1-09` adds qualification resolution tests for inclusive validity, strongest attributable coverage, confirmation requirements, future/superseded records, apprentice signals, stable order-independent fingerprints, and expiry attention phases. `P1-10` adds customer-relationship resolution tests for deterministic typed-timeline ordering/cursors, per-document deduplication, communication-default/override resolution, warning requirements, follow-up attention visibility/capacity ordering, strict offset-aware timestamps, deterministic Europe/Berlin display/edit conversion across DST ambiguity, whitespace-normalized input validation, and safe Realtime event coalescing/filtering. `P1-11` adds planning tests for Berlin wall-clock conversion (ordinary time, spring gap shift, first autumn occurrence), elapsed cross-midnight/DST duration splitting, weekly/monthly materialization, invalid monthly-date skipping, multi-day all-day bounds, stable occurrence identity/idempotence, combined same-day demand, and distinct effective/provisional/overlap/over-capacity explanations with deterministic fingerprints. `P1-12` adds dispatch tests across four files: acknowledgement derivation (latest-row-wins with parsed-instant comparison and id tie-breaks, the non-login „nicht möglich" state, open versus kept-resolved challenges returning a recipient to pending) and explicit-fact travel notes (same-site pass, zero/negative-gap warnings, „nicht bewertet" gaps, per-employee/per-day independence) in `lib/dispatch/derivation.test.ts`; honest readiness composition (capacity/qualification separation, missing versus load-failed site, travel warn/unknown, material open-quantity coverage incl. exact cover always labeled „nicht reserviert", tools always unknown, the fixed audit dimension order) in `lib/dispatch/readiness.test.ts`; batch-shift math (wall-time preservation across both DST transitions, uniform new times, all-day boundary shifts, no-op/invalid/all-day-needs-day-shift/non-positive-duration rejections with per-item attribution) in `lib/dispatch/batch.test.ts`; and commitment mismatch rules (inclusive arrival-window bounds, date moves, all-day satisfaction, German window formatting) in `lib/commitments/types.test.ts`. The P1-12 acceptance checkpoint passed 187/187.

P1-13 adds `lib/work-templates/schemas.test.ts` for bounded drafts, blank normalization, cross-reference validation, dependency self/cycle rejection, unique identities and positive planned quantities. P1-14 adds `lib/work-lifecycle/types.test.ts` for the fixed transition graph, employee limits, next-action precedence, snapshot validation and fingerprint bounds. P1-15 adds `lib/work-artifacts/validation.test.ts` and `lib/work-artifacts/export.test.ts` for kind-specific validation, immutable action context and deterministic export identity. P1-16 adds field projection/terminal, fail-closed dispatch and signer-privacy tests. P1-17 adds deterministic canonical customer-package export, stable absent-package identity, project handover derivation and the extended lifecycle-gate cases. P1-18 adds bounded equipment/component validation, identifier normalization and exact equality, lifecycle transition/correction rules, field-projection privacy and date-only display ownership. P1-19 adds service-case validation, relation invariants, field-projection privacy and lifecycle/charge-context tests. P1-20 adds maintenance-plan, operational-coverage and visit-completion validation, month-end clamping cases and exact contextual-upload target tests. P1-21 adds canonical segment totals, legacy-state projection, work/break transition ordering and break-close cases. The hardening pass adds run-policy, run-inventory, migration-history, CLI-link safety, file-lock, Realtime parity, Playwright lint-rule, spec-convention, and document-upload target coverage. The current suite passes **435/435 tests across 41 files with 789 expectations**.

## What Is Not Covered Yet

Playwright discovery finds **132 Golden tests in 22 files**, **117 audit tests in 16 files**, and **9 canary tests in 1 file**. `p1-21.spec.ts` is the last-sorting staged Golden spec with four persisted stage tags; its Wave 2 audit maps all 64 catalog flows and owns run-day +110 through +114. The unit suite contains **435 tests across 41 files**. The complete evidence record lives in `docs/plans/golden-gate-log.md`. Future specs must address dispatch, template/application, lifecycle, artifact, evidence, time, material, blocker, handover/package, installed-equipment, service-case, maintenance and canonical time records by run-owned identities rather than assume any domain is empty.

### Live-provider limits after the deterministic/canary split

Decision [0006](../decisions/0006-testing-architecture.md) resolved the old structural limit: the long serial batteries no longer depend on live cloud providers, so a network hiccup can no longer fail an application certification. What remains true:

- A **cloud-target** run (canary, wave-end battery) still carries the provider dependency. Preflight cannot guarantee a later network window; classify environment failures honestly and never retry availability luck into evidence.
- The local stack is deterministic infrastructure, not magic: WSL, Docker, and machine load still produce environment-class failures (the WSL address changes on restart, `db reset` stops the edge runtime, heavy load still stretches budgets). The preflight catches the known ones with named remedies; classify anything new before rerunning.
- One green run still proves one ordering. The full battery remains a certification signal, not an iteration loop.

The Golden and audit configs use a 180-second per-test budget for local runs and a 300-second budget for cloud-target runs. The canary always uses 300 seconds. Spec files have no per-test overrides: ESLint rejects `test.setTimeout()` and `test.slow()`. Realtime-sensitive mutation helpers may retry one complete transaction only when a refresh demonstrably removed the UI before submission; after submission they must reassert persisted state rather than repeat the mutation. A missing live refresh may use one reload only where the authoritative persisted row is still asserted. These guards keep the business assertions strict without turning delivery latency into duplicate writes.
