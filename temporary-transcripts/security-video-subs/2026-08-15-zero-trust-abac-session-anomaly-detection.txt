# Attribute-based access control and session anomaly detection

Source: https://www.instagram.com/reel/DcEEyocD8II/
Creator: @mattmurphyai
Posted: 2026-08-15
Topic: security-video-subs

## Summary

An attacker logged into your app at 3 AM from another country.

## Transcript

A hacker logged into your app at 3am from another country.
Your app said welcome back.
Stolen credentials for an IP middle of the night.
So your app cannot tell the difference between your real user and the person who has stolen
their password.
It's the same role, same permission, same access to everything.
Because your AI built static roles that never evaluate context in the moment.
So here's what tier 3 RBAC actually looks like for your app.
Step one, attribute based access control.
Permissions that evaluate context, not just the role.
What time of day it is, what location, where the device fingerprint is, what's the IP
reputation, what's the data sensitivity level.
So direct your AI to build a policy engine that evaluates these attributes on every single
request.
A user accessing financial records at 3am from an unrecognized device gets stepped up
authentication or denied entirely.
Same role, different context, different decision.
That's definitely a win.
Step two, zero trust enforcement on every internal request.
Not just the logging gate, every API call, every database query.
So every service to service request that reverifies identity and authorization.
Your AI trusts everything inside the network perimeter.
Zero trust assumes there is no perimeter.
Every request who proves it is or gets rejected.
And step three, continuous session risk scoring.
Not a one time check at login, a running evaluation that monitors behavior throughout their session.
If a user's behavior pattern shifts mid session, the system challenges or terminates automatically.
So direct your AI to build session anomaly detection that watches for impossible travel,
unusual data access volume or privilege escalation attempts in real time.
Static roles tell you who someone else is.
Context tells you whether to trust them right now or not.
So direct your AI to build for both.

## Orchestration prompt

Source: reconstructed from the directives spoken in the video.

1. Build a policy engine that evaluates these attributes on every single request.
2. Build session anomaly detection that watches for impossible travel, unusual data access volume or privilege escalation attempts in real time.
